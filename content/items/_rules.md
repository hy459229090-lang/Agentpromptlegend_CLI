# items Rules

1. Item IDs must start with `item_`; affix IDs must start with `affix_`.
2. Tiers must be `common`, `heroic`, or `legendary`.
3. Legendary items must define allowed effects.
4. Do not add unbounded model freedom.


# assets Rules

1. Runtime assets must be local files committed to the repo or installed package.
2. Do not store unreviewed generated candidates here.
3. Every runtime-enabled asset must have QA status `qa_passed`.
4. Every bitmap asset must have Unicode cell and ASCII-safe fallback mapping.
5. Do not include secrets, local player data, or third-party brand replicas.

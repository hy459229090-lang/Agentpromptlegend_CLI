# source_briefs Rules

1. Briefs describe development-time candidate generation only.
2. Do not store generated candidate images in this directory.
3. Every alias must map to one asset manifest `source_brief`.
4. Prompt text must forbid logos, text UI, brand replicas, and unrelated props.
5. Runtime assets still require `qa_passed` entries in `content/assets/manifest.yaml`.

# content/assets/runtime

QA-approved runtime bitmap assets live here.

Only promoted candidates with a matching `qa_passed` record may be stored in
this directory. Runtime metadata must use portable relative frame paths, and
the owning manifest entry must keep an ASCII/Unicode fallback mapping.

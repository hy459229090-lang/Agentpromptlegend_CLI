# cand_skill_hex_seal_001

QA-promoted 12-frame Hex Seal effect cut.

`cut_metadata.yaml` is the runtime entry point. Frame PNG paths are deliberately
relative so installed packages and other machines can load the asset.

# tests Rules

1. Default tests must not require network or API keys.
2. Use mock provider for model behavior.
3. Snapshot tests should use ASCII-safe/no-color output.
4. Test names should map to `REQ-*` where practical.


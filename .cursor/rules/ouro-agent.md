# Cursor Rule — Ouro Agent

Always read `AGENTS.md` and `docs/product/06_需求追踪矩阵_20260503.md` before editing.

Implement only ready `REQ-*` items. For the first pass, stay within Slice 0 and Slice A.

Do not store API keys in plaintext. Do not implement uploads, leaderboards, multiplayer, or real provider calls before mock battle validation.

Default UI must be ASCII-safe and testable. Unicode, color, and animation are optional enhancements.


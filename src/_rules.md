# src Rules

1. Do not store content data or runtime traces here.
2. Do not hardcode API keys or user config.
3. Keep modules importable without network access.
4. Mock mode must remain available without provider SDKs.


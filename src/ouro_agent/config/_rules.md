# config Rules

1. Never store plaintext API keys.
2. Store environment variable names such as `OPENAI_API_KEY`, not values.
3. `mock` must be the safe default provider.
4. Config errors must be user-readable.


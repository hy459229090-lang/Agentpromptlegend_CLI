# ouro_agent Rules

1. Keep provider-specific code out of `engine`.
2. Keep terminal rendering out of `engine`.
3. Keep content data outside `src`.
4. Use stable IDs from `docs/product/02_设计基线与术语表_20260503.md`.


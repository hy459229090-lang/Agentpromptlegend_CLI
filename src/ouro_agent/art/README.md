# art

Terminal glyphs, ASCII avatars, status symbols, sprite atlas contracts, and
animation timelines used by TUI.

Design source is under `docs/product/art/`. Bitmap/cell runtime assets are
loaded through `sprite_atlas.py` after QA; `battle_assets.py` remains the
ASCII-safe fallback registry.

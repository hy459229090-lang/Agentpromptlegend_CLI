# Ouro Agent 需求追踪矩阵

> 版本：v0.9
> 日期：2026-05-16
> 用途：把策划案转成 coding 工具可执行、可验收、可追踪的需求。
> 当前范围：覆盖已完成 Slice 0/A/B/Provider，并追加下一阶段 BattleLLMSession、Build/UI、怪物图鉴、内容扩展、游戏性可玩性迭代、界面美术、数值联合实施、战斗演出与 TUI 架构需求。

---

## 1. 字段说明

| 字段 | 含义 |
|------|------|
| ID | 稳定需求 ID |
| 来源 | 对应策划文件 |
| 玩家价值 | 这条需求服务的体验 |
| 系统行为 | 程序必须做什么 |
| 验收标准 | 怎么判断完成 |
| 证据 | 测试、trace、日志、截图或试玩记录 |
| 优先级 | P0/P1/P2/P3 |
| 切片 | Slice A-F |
| 状态 | draft/ready/in_progress/done/blocked |

---

## 2. Slice 0 发布与配置骨架需求

| ID | 来源 | 玩家价值 | 系统行为 | 验收标准 | 证据 | 优先级 | 切片 | 状态 |
|----|------|----------|----------|----------|------|--------|------|------|
| REQ-DIST-001 | G15 | 玩家能从 GitHub 安装 | 建立可安装 CLI 包骨架 | GitHub 安装后 `ouro --version` 可运行 | `venv312/bin/ouro --version` 输出 `ouro 0.1.0`; clean venv `/private/tmp/ouro_install_smoke_20260601/bin/python -m pip install .` 成功构建并安装 wheel；clean venv `ouro --version` 输出 `ouro 0.1.0`、`ouro doctor --lang en --content-dir content` 输出 content OK、`ouro --lang en play --mock --seed 1 --no-animation --no-trace --content-dir content` 输出回合帧和 `Codex : /private/tmp/ouro_install_smoke_home_doctor/codex.json`; `tests/unit/test_docs.py` | P0 | 0 | done |
| REQ-DIST-005 | G15/G16 | 安装后的 CLI 离开源码目录也能试玩 | 打包时把结构化内容 YAML 安装到 `share/ouro-agent/content`；默认 `content` 不存在时回退到安装内容；显式 `--content-dir` 仍优先 | clean venv 从 `/private/tmp` 运行 `ouro doctor` 不带 `--content-dir` 输出 content OK；`ouro play --mock` 不带 `--content-dir` 可完成战斗并写 Codex | `tests/unit/test_content_loader.py::test_resolve_default_content_dir_falls_back_to_installed_share`、`test_resolve_content_dir_preserves_explicit_custom_path`；clean venv `/private/tmp/ouro_install_smoke_20260601/bin/ouro --lang en doctor`（cwd `/private/tmp`）输出 `content dir  : /private/tmp/ouro_install_smoke_20260601/share/ouro-agent/content`；clean venv `ouro --lang en play --mock --seed 1 --no-animation --no-trace`（cwd `/private/tmp`）输出 `BATTLE COMPLETE` 和 `Codex : /private/tmp/ouro_non_repo_install_check/codex.json` | P0 | 0/Release | done |
| REQ-DIST-002 | G15 | 没有 API key 也能试玩 | 默认 provider 为 mock | `ouro play --mock` 不读取 API key | `tests/unit/test_config.py::test_default_provider_is_mock`; `tests/integration/test_mock_battle.py::test_cli_play_prints_turn_frames_and_report` | P0 | 0 | done |
| REQ-DIST-003 | G15 | 玩家能按 README 上手 | README 包含安装、mock 试玩、Provider 配置、隐私说明 | 新用户按 README 能跑 mock | `tests/unit/test_docs.py::test_readme_covers_install_mock_provider_and_privacy` | P0 | 0 | done |
| REQ-DIST-004 | Code Layout | 代码后续可维护 | 建立 src/content/tests/docs/examples/scripts 目录骨架并为每个目录提供 README 和 `_rules.md` | 目录存在且职责边界清楚 | `tests/unit/test_docs.py::test_required_layout_directories_have_readmes_and_rules`; `venv312/bin/ouro --lang en doctor` 输出 | P0 | 0 | done |
| REQ-DIST-006 | Packaging | 发布元数据可信 | `pyproject.toml` 中 `package-data` 和 `data-files` 引用的文件都真实存在；类型标记 `py.typed` 随包提供 | wheel 构建前即可发现缺失的 package data 或 bundled content 引用；`ouro_agent/py.typed` 存在 | `tests/unit/test_docs.py::test_pyproject_packaging_file_references_exist`；`src/ouro_agent/py.typed`；`/private/tmp/ouro_install_smoke_20260601/bin/python -m pip install .` | P1 | Release | done |
| REQ-DIAG-001 | G15/G16 | 新玩家和发布前能一条命令确认安装可用 | `ouro doctor` 尊重 `--lang`，展示版本、配置、provider、内容包计数、mock 可玩状态；内容目录无效时返回非零 | `ouro doctor --lang en --content-dir content` 显示 `language : en`、content OK 与内容数量；坏内容目录返回 3 并显示 content err | `tests/unit/test_config.py::test_doctor_respects_language_override_and_validates_content`、`tests/unit/test_config.py::test_doctor_fails_when_content_is_invalid`；`venv312/bin/ouro doctor --lang en --content-dir content` 输出 `content : OK heroes=6 skills=18 enemies=9 items=15 affixes=12 resonances=5`；`venv312/bin/ouro --lang en doctor --content-dir /private/tmp/ouro_missing_content_20260601` 返回 3 并输出 `content err` | P0 | 0 | done |
| REQ-DIAG-002 | G15/G09 | `doctor` 能发现真实 Provider 未就绪而不是只检查内容 | `ouro doctor` 集成 provider preflight，显示 provider check 状态和问题；mock 默认 ready；真实 provider 缺 env/model/base_url 时返回非零且不回显 key 值 | 默认 mock doctor 返回 0 并显示 `provider chk : READY`；真实 provider env 存在时只显示 `set (hidden)`；真实 provider 缺 env 时返回 3 并显示 `provider err` | `tests/unit/test_config.py::test_doctor_respects_language_override_and_validates_content`、`test_doctor_reports_real_provider_preflight_without_leaking_env`、`test_doctor_returns_not_ready_when_real_provider_env_is_missing`；`venv312/bin/ouro doctor --lang en --content-dir content` 输出 `provider chk : READY`；真实 provider 缺 env 的 doctor 输出 `provider chk : NOT READY` 与 `provider err`；`venv312/bin/python scripts/release_check.py` 输出 `350 passed` | P1 | Provider/Release | done |
| REQ-PROV-001 | G15 | 玩家能选择模型供应商 | 配置层支持 mock/openai/anthropic/openai-compatible | `ouro config set provider <name>` 可设置合法 provider | `tests/unit/test_config.py::test_set_provider_accepts_supported`; `venv312/bin/ouro --lang en doctor` 输出 | P0 | 0 | done |
| REQ-PROV-002 | G15 | 玩家能配置 API URL | 支持 `base_url` 和 `model` 配置 | `ouro config show` 显示 provider/model/base_url，不显示 key | `tests/unit/test_config.py::test_redacted_view_never_includes_secret`; `tests/unit/test_config.py::test_config_setup_prompts_step_by_step` | P0 | 0 | done |
| REQ-PROV-003 | G15 | API key 不泄露 | 只保存环境变量名，不保存明文 key | 配置文件不包含 key 明文 | `tests/unit/test_config.py::test_save_does_not_persist_secrets`; `tests/unit/test_config.py::test_load_rejects_legacy_plaintext_key` | P0 | 0 | done |
| REQ-PROV-004 | G15/G05/G16 | 玩家配置的 Provider 真正进入游戏命令 | `play` / `run` 从 `load_config()` 派生有效配置；`--mock` 才强制 mock；不再写死某个真实 provider、model、base_url 或 API env | 保存 `openai-compatible` 配置后，非 `--mock` 的 `play` 把 provider/model/api_key_env/base_url/timeout/retry 传给 provider 构建器；`--mock` 保留无 key 可玩 | `tests/unit/test_config.py::test_game_config_uses_saved_provider_unless_mock`；`tests/integration/test_mock_battle.py::test_cli_play_uses_saved_provider_config_when_not_mock`；`rg -n "Hardcoded API configuration" src/ouro_agent/cli/main.py` 无输出 | P0 | 0/Provider | done |
| REQ-ART-001 | G14 | 游戏像终端游戏而不是日志 | 支持 ASCII-safe 主屏输出 | 战斗屏在 PowerShell 可读 | `tests/integration/test_mock_battle.py::test_battle_screen_is_ascii_safe`; `tests/unit/test_i18n.py::test_battle_screen_en_remains_ascii_safe` | P0 | 0 | done |
| REQ-CONTENT-001 | G13 | 角色有辨识度 | 至少提供暗影学徒的角色卡和 ASCII 头像 | 英雄选择或战斗面板能显示角色信息 | `tests/unit/test_content_loader.py::test_load_default_content_bilingual`; `tests/unit/test_build.py::test_render_hero_card_zh`; `venv312/bin/ouro --lang en validate-content` 输出 | P1 | 0 | done |

---

## 3. Slice A 需求

| ID | 来源 | 玩家价值 | 系统行为 | 验收标准 | 证据 | 优先级 | 切片 | 状态 |
|----|------|----------|----------|----------|------|--------|------|------|
| REQ-BTL-001 | G02 | 战斗自动推进，有读条节奏 | 实现英雄和敌人的 ATB 推进 | 单场战斗中单位能按 speed 获得行动机会 | `tests/unit/test_battle.py::test_atb_progression_is_deterministic`; `tests/integration/test_mock_battle.py::test_fixed_seed_is_repeatable` | P0 | A | done |
| REQ-BTL-002 | G02 | 英雄有资源管理 | 实现 HP、MP、技能冷却 | MP 不足或冷却未好时技能不可用 | `tests/unit/test_battle.py::test_skill_consumes_mp_and_sets_cooldown`; `tests/unit/test_battle.py::test_skill_blocked_when_no_mp`; `tests/unit/test_battle.py::test_skill_blocked_when_on_cooldown` | P0 | A | done |
| REQ-BTL-003 | G02 | 战斗结果可信 | 本地裁判结算伤害、状态、胜负 | 模型不能直接决定伤害或胜负 | `tests/unit/test_battle.py::test_unknown_skill_falls_back_via_judge`; `tests/unit/test_battle.py::test_full_mock_battle_resolves`; `tests/integration/test_mock_battle.py::test_trace_is_written_locally` | P0 | A | done |
| REQ-BTL-004 | G02 | 敌人能自动行动 | 实现敌人规则 AI | 敌人能选择合法行动并造成结算 | `tests/unit/test_battle.py::test_silence_breaks_chant_locally`; `tests/integration/test_mock_battle.py::test_fixed_seed_is_repeatable` trace evidence | P0 | A | done |
| REQ-BTL-005 | G02 | 战斗能结束 | 实现胜利、失败、异常保护 | 敌人全灭胜利，英雄死亡失败 | `tests/unit/test_battle.py::test_full_mock_battle_resolves`; `tests/unit/test_battle.py::test_batch_run_produces_reproducible_results` | P0 | A | done |
| REQ-LLM-001 | G05 | AI 是英雄决策核心 | 英雄行动时通过模型 action schema 选择行动 | 每次英雄行动都有结构化 action | `tests/unit/test_action_validator.py::test_valid_action_parses`; `tests/integration/test_mock_battle.py::test_trace_is_written_locally` | P0 | A | done |
| REQ-LLM-002 | G05 | 模型错误不会毁局 | 实现 JSON 修复、降级、惩罚流程 | 可修复、非法、不可读样例都有处理 | `tests/unit/test_action_validator.py::test_repairable_json_is_repaired`; `tests/unit/test_action_validator.py::test_unparseable_text_falls_back_to_defend`; `tests/unit/test_action_validator.py::test_unknown_action_type` | P0 | A | done |
| REQ-LLM-003 | G05 | Prompt 影响决策 | Prompt Composer 注入英雄身份、Build、战况、图鉴、玩家 Prompt | 生成的 prompt 包含必需区块，不含隐藏信息 | `tests/unit/test_prompt.py::test_prompt_injects_visible_identity_build_battle_and_player_prompt`; `tests/unit/test_codex.py::test_prompt_codex_visibility_is_stage_and_tier_limited` | P0 | A | done |
| REQ-LLM-004 | G05 | 无 API key 也能玩 | 实现 mock model | mock model 可跑完整战斗 | `tests/unit/test_config.py::test_default_provider_is_mock`; `tests/unit/test_battle.py::test_full_mock_battle_resolves`; `tests/integration/test_mock_battle.py::test_cli_play_prints_turn_frames_and_report` | P0 | A | done |
| REQ-DATA-001 | G08 | 内容可扩展 | 英雄、技能、敌人从结构化数据加载 | 修改数据不需要改战斗逻辑 | `tests/unit/test_content_loader.py::test_load_default_content_bilingual`; `venv312/bin/ouro --lang en validate-content` 输出 | P0 | A | done |
| REQ-DATA-002 | G08 | 内容不乱引用 | 实现 ID、tier、node type、技能引用校验 | 悬空引用会报错 | `tests/unit/test_content_loader.py::test_dangling_skill_reference_fails`; `tests/unit/test_content_loader.py::test_invalid_id_prefix_rejected`; `venv312/bin/ouro --lang en validate-content` 输出 | P0 | A | done |
| REQ-TRC-001 | G09 | 行为可复盘 | 每场战斗输出本地 trace | trace 包含 run_id、battle_id、turn_id、action、裁判结果 | `tests/integration/test_mock_battle.py::test_trace_is_written_locally`; `tests/integration/test_mock_battle.py::test_battle_llm_session_ids_static_hash_and_delta_are_traced` | P0 | A | done |
| REQ-TRC-002 | G09 | 玩家隐私可控 | 默认不上传 trace，不保存明文 API key | mock mode 不需要 API key，配置不写 key 明文 | `tests/unit/test_config.py::test_save_does_not_persist_secrets`; `tests/integration/test_mock_battle.py::test_trace_is_written_locally` | P0 | A | done |
| REQ-VAL-001 | G10 | 第一轮可验证 | 提供固定 seed 或可重复 mock 战斗 | 同一 seed/mock 结果可复测 | `tests/integration/test_mock_battle.py::test_fixed_seed_is_repeatable`; `tests/unit/test_battle.py::test_batch_run_is_reproducible_with_same_seed` | P0 | A | done |
| REQ-REPLAY-001 | G09/G14/G16 | 玩家能复盘模型行动和本地裁判 | 提供 `ouro replay <trace>` 读取本地 JSONL trace 并输出时间线、模型摘要、裁判结果、Echo 成本 | 示例 trace 和新生成 trace 都能在无网络情况下回放；默认不输出 API key 或隐藏配置 | `tests/unit/test_replay.py` 覆盖示例 trace、模型摘要、CLI `replay`、坏 JSON；`venv312/bin/ouro replay examples/traces/mvp_a_seed7_mock.trace.jsonl --limit 6 --lang en` 输出 Timeline、Judge、Echo Cost、Result | P1 | C | done |
| REQ-REPLAYDIR-001 | G22/G09/G14/G16 | 回放页先给导演摘要，再进入逐事件时间线 | `replay` 输出新增 `REPLAY DIRECTOR BOARD`，汇总 hero/enemy 回合数、Prompt、首个英雄行动、结果和 Echo/Ritual Time | 玩家打开 trace 后能先判断这场回放的复盘价值；原 Timeline、Judge、模型摘要和 Echo 成本保留；不泄露 API key | `tests/unit/test_replay.py::test_trace_replay_renders_example_timeline`、`test_trace_replay_shows_model_summary_and_result` 覆盖 `REPLAY DIRECTOR BOARD`、`[TURNS]`、`[PROMPT] control`、`[FIRST HERO]`、`[RESULT] victory @ tick 31`、`[ECHO] Echo Cost 9 / Ritual Time 42ms`；`venv312/bin/ouro replay examples/traces/mvp_a_seed7_mock.trace.jsonl --limit 6 --lang en` 输出 replay director board | P0 | C-Polish/E | done |
| REQ-REPLAYBEAT-001 | G22/G09/G14/G20 | 回放不只列事件，还要先显示英雄/敌人行动节奏图 | `replay` 在导演板后新增 `REPLAY BEAT MAP`，用 `H009 -> E010 -> ...` 表示英雄/敌人行动轨道，并汇总 tick span、事件数、英雄输出和敌方伤害压力 | 策划打开 trace 后能先判断前段节奏、完整战斗跨度和隐藏尾部，再读 Timeline；limit 截断时显示 `+N` 隐藏事件；不泄露模型密钥 | `tests/unit/test_replay.py::test_trace_replay_renders_example_timeline` 覆盖 `REPLAY BEAT MAP`、`[FLOW] H009 -> E010`、`[SPAN] tick 9->209`、`[PRESSURE] hero dealt`、`[READ] H=hero action`；`test_trace_replay_shows_model_summary_and_result` 覆盖 `H012 -> E016` 和 `hero dealt 16`；`venv312/bin/ouro replay examples/traces/mvp_a_seed7_mock.trace.jsonl --limit 4 --lang en` 输出 beat map | P0 | C-Polish/E | done |
| REQ-CODEX-002 | G07/G09/G16 | 图鉴经验跨战斗和跨运行保留 | 战斗遭遇/击败怪物写入本地 codex 存档；Codex reward 追加研究进度；下一场 Prompt 读取已解锁信息 | `ouro play --mock` 后生成本地 codex 存档；同一家族/tier 的观察和击败计数可复用；不保存 API key；本地存档不可写时不打断 mock play | `tests/unit/test_codex.py::test_codex_progress_store_round_trip`、`test_record_battle_codex_tracks_encounter_and_defeat`、`test_saved_codex_progress_enters_next_battle_prompt`、`test_codex_store_wraps_write_failures`；`tests/unit/test_run_state.py::test_run_state_records_battle_and_codex_reward_progress`；`tests/integration/test_mock_battle.py::test_cli_play_persists_codex_progress`、`test_cli_play_warns_when_codex_save_is_unavailable`；`env OURO_AGENT_HOME=/private/tmp/ouro_codex_check venv312/bin/ouro --lang en play --mock --seed 1 --no-animation --no-trace` 输出 `Codex : /private/tmp/ouro_codex_check/codex.json`；`env OURO_AGENT_HOME=/private/tmp/ouro_blocked_home_file_20260601 venv312/bin/ouro --lang en play --mock --seed 1 --no-animation --no-trace` 输出 `Warning: could not save Codex progress` 且战斗完成 | P1 | E | done |
| REQ-RUNSAVE-001 | G09/G16 | 玩家死亡或通关后留下可回看的长期记录 | `run` 结束时写入本地 run archive；死亡/超时追加 death history；记录 hero、dungeon、seed、Build、资源、节点、战斗数、结果 | `ouro run --mock --auto` 后生成本地 run JSON；失败 run 会进入 death history；不保存 API key；长期记录不可写时用警告降级而不是 traceback | `tests/unit/test_archive.py` 覆盖 run archive、death history、坏 JSON、`test_run_archive_wraps_write_failures`；`env OURO_AGENT_HOME=/private/tmp/ouro_run_archive_check venv312/bin/ouro --lang en run --mock --auto --seed 7 --no-animation --no-trace` 失败结算输出 `Run Archive:` 和 `Death History:`；`/private/tmp/ouro_run_archive_check/runs/run_1780282288.run.json` 记录 hero/dungeon/seed/Build/resources/nodes/codex 且无 `sk-`；默认 home 不可写时 `venv312/bin/ouro --lang en run --mock --auto --seed 7 --no-animation --no-trace` 仍输出 `YOU DIED`、`Warning: could not save run archive`、`Warning: could not save death history`，无 traceback | P1 | E | done |
| REQ-LONGVIEW-001 | G09/G14/G16 | 玩家能在 CLI 中回看长期进度 | 新增 `ouro codex [enemy_id]` 和 `ouro history`，直接展示本地图鉴进度、怪物详情与死亡历史 | 无网络、无 API key 时可查看；空历史有可读空状态；图鉴详情使用已持久化阶段；死亡历史按最近记录展示 seed、英雄、副本、Build、战斗数和路线 | `tests/unit/test_codex.py::test_cli_codex_shows_persisted_summary`、`test_cli_codex_shows_persisted_detail`；`tests/unit/test_archive.py::test_cli_history_shows_persisted_deaths`、`test_cli_history_shows_empty_state`；`venv312/bin/ouro codex --lang en --content-dir content` 输出 `CODEX :: MONSTER ARCHIVE`；`venv312/bin/ouro history --lang en --limit 5 --content-dir content` 输出 `DEATH HISTORY :: FALLEN RUNS` 或空历史文案 | P1 | E | done |
| REQ-DEATHREVIEW-001 | G22/G09/G16/G18 | 死亡历史能指导下一局重试，而不是只记录失败 | `history` 输出新增 `DEATH REVIEW BOARD`，汇总死亡样本数、最近死亡、最深推进和 control Prompt 重试命令 | 有死亡记录时先展示复盘板；空历史保持可读空状态；输出不泄露 API key | `tests/unit/test_archive.py::test_cli_history_shows_persisted_deaths` 覆盖 `DEATH REVIEW BOARD`、`[SAMPLES]`、`[LATEST] seed 12`、`[DEEPEST] seed 12`、`[RETRY] ouro run --mock --prompt-style control --seed 13`；`venv312/bin/ouro history --lang en --limit 5 --content-dir content` 输出 death review board | P0 | C-Polish/E | done |
| REQ-LONGVIEW-002 | G09/G14/G16 | 玩家能在 CLI 中回看全部运行归档而不只看死亡 | 新增 `ouro runs --limit N`，读取本地 `runs/*.run.json` 并展示 seed、结果、英雄、副本、Build、资源、战斗数、节点数和归档路径 | 无网络、无 API key 时可查看；空归档有可读空状态；坏归档 JSON 报可读错误；主菜单提供 Runs 入口 | `tests/unit/test_archive.py::test_cli_runs_shows_persisted_archives`、`test_cli_runs_shows_empty_state`、`test_run_archives_reject_invalid_json`；`venv312/bin/ouro runs --lang en --limit 5 --content-dir content` 输出 `RUN ARCHIVE :: ALL RUNS`；`tests/unit/test_config.py::test_cli_default_shows_main_menu` 覆盖 `Runs` | P1 | E/Release | done |
| REQ-RUNARCHIVEBOARD-001 | G22/G09/G16/G18 | 运行归档列表能先比较战绩并引导复盘 | `runs` 输出新增 `RUN ARCHIVE BOARD`，汇总 complete/dead、最佳 seed、最新结果和 `run-report` 命令 | 玩家打开归档列表即可判断哪局最值得复盘；空状态保持可读；不泄露 API key | `tests/unit/test_archive.py::test_cli_runs_shows_persisted_archives` 覆盖 `RUN ARCHIVE BOARD`、`[RESULTS] complete 1 / dead 1`、`[BEST] seed 21 / 3W/0L`、`[LATEST] seed 22 / dead`、`[REPORT] ouro run-report`；`venv312/bin/ouro runs --lang en --limit 5 --content-dir content` 输出 run archive board | P0 | C-Polish/E | done |
| REQ-LONGVIEW-003 | G09/G14/G16 | 回流玩家一条命令知道自己进度和下一步 | 新增 `ouro status`，汇总 provider/profile、本地图鉴解锁、run archive 总数、death history 总数、最近一次 run 与下一步命令 | 无网络、无 API key 时可查看；输出不泄露 key；安装版 smoke 覆盖 `ouro status`；主菜单与 demo 下一步命令提供 Status 入口；80/100/120 列中英文无溢出 | `tests/unit/test_archive.py::test_cli_status_summarizes_progress_and_next_commands`；`tests/unit/test_config.py::test_cli_default_shows_main_menu`、`test_cli_demo_runs_guided_mock_smoke`；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖 status 宽度；`venv312/bin/python scripts/release_check.py --install-smoke-only` 输出 `OURO STATUS :: ECHO LEDGER`、`Runs: 1 total`、`Fallen Runs: 1`、`ouro run --mock` | P1 | E/Release | done |
| REQ-LONGVIEW-004 | G09/G16/G18 | 状态页给出下一局计划而不只是命令列表 | `ouro status` 根据本地 Codex、最近 run 结果和死亡历史输出 1-3 条 Next Run Plan；死亡后提示复盘、控制型 Prompt 与路线/休息调整；空档案提示 guided demo/mock run | 建议只来自本地归档和图鉴，不联网、不读取 API key；中英文和 80/100/120 列宽下不溢出；死亡、通关、空档案都有可读计划 | `tests/unit/test_archive.py::test_cli_status_summarizes_progress_and_next_commands` 覆盖空档案 guided/demo、通关 winning build/guarded seed、死亡后 `NEXT RUN PLAN`、`ouro history --lang en --limit 3`、`ouro run --mock --prompt-style control`、Codex 缺口和 boss 前 rest/shop 建议；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖 status 下一局计划在 80/100/120 中英文无溢出；`venv312/bin/python scripts/release_check.py --install-smoke-only` 检查安装版 `NEXT RUN PLAN` 和 control prompt 建议 | P1 | C-Experience/Release | done |
| REQ-STATUSCTRL-001 | G22/G09/G16/G18 | 回流玩家打开 status 后直接看到下一局配置槽 | `ouro status` 在 Next Run Plan 后新增 `NEXT RUN CONTROL`，把 Prompt、Seed、Codex、Route/Run 拆成控制槽；空档案、死亡、通关分别给不同配置 | 空档案给 default/seed7/first run；死亡给 control、固定重试 seed、Codex 缺口、rest/shop；通关给 guarded、压力 seed、elite/event greed；80/100/120 中英文不溢出 | `tests/unit/test_archive.py::test_cli_status_summarizes_progress_and_next_commands` 覆盖空档案 `NEXT RUN CONTROL`、`[PROMPT] default`、`[RUN] ouro run --mock --seed 7`、通关 guarded/elite-event、死亡 control/seed32/rest-shop；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖 status 中英文宽度矩阵和 `下一局控制台`；`venv312/bin/ouro status --lang en --content-dir content` 输出 next run control | P0 | C-Polish/E | done |
| REQ-STATUSMAP-001 | G22/G09/G16/G18/art08 | 状态页长期成长不能只是数字清单，要像玩家进度地图 | `ouro status` 在 Progress 后新增 `LEGEND PROGRESS MAP`，用条形轨道展示 Codex 掌握度、通关率、陨落压力和下一局准备信号 | 回流玩家能一眼看出收集进度、run 结果压力和下一局是否有足够复盘信号；空档案、死亡归档、中英文 80/100/120 宽度稳定；不读取网络或 API key | `tests/unit/test_archive.py::test_cli_status_summarizes_progress_and_next_commands` 覆盖 `LEGEND PROGRESS MAP`、`[CODEX]`、`[RUNS]`、`[RISK] fall pressure`、`[READY] next-run signals`；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖英文 `LEGEND PROGRESS MAP` 与中文 `传奇进度地图`；`venv312/bin/ouro status --lang en --content-dir content` 输出 legend progress map | P0 | C-Polish/E | done |
| REQ-LONGVIEW-005 | G09/G16/G18 | 打完一局后能用短报告复盘成败和下一局动作 | 新增 `ouro run-report [archive_path]`，默认读取最新 run archive，也可读取指定 `.run.json`，输出结果、seed、英雄、副本、Build、资源、路线、Codex 进度和下一局建议 | 无网络、无 API key 时可查看；空归档有可读空状态；坏归档沿用可读错误；默认报告不展开完整长日志；主菜单、demo 和 status 下一步命令提供 Run Report 入口 | `tests/unit/test_archive.py::test_cli_runs_shows_persisted_archives` 覆盖 latest/path 报告、空状态和不泄露 key；`venv312/bin/ouro run-report --lang en --content-dir content` 输出 `RUN REPORT :: LAST ECHO` 或空状态；`venv312/bin/python scripts/release_check.py --install-smoke-only` 输出 `RUN REPORT :: LAST ECHO`、`NEXT RUN` 和 control prompt 建议 | P1 | E/Release | done |
| REQ-CLIUX-001 | G15/G16/G18 | 试玩脚本不会把正常死亡误判为程序崩溃 | `ouro run` 默认在一局可玩会话正常结束时返回 0；死亡/超时仍在屏幕、run archive 和 death history 中表达；需要 CI 按胜负失败时使用 `--strict-result-exit-code` | 固定 seed 死亡 run 返回 0 且输出 `YOU DIED`、`Run Archive:`、`Death History:`；严格模式下 DEAD run 返回 1，COMPLETE run 返回 0 | `tests/integration/test_mock_battle.py::test_cli_run_death_returns_successful_session_code`；`tests/unit/test_run_state.py::test_run_exit_code_treats_death_as_played_session_by_default`；`env OURO_AGENT_HOME=/private/tmp/ouro_exit_code_check venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace --content-dir content` 返回 `0` 并输出死亡结算 | P1 | E/Release | done |
| REQ-CLIUX-002 | G15/G16 | 交互式命令取消不会杀掉嵌入式调用者 | 所有 CLI 输入点通过可捕获输入 helper 处理 Ctrl-C/EOF；`main()` 返回 130，只有 `python -m` 最外层转成进程退出 | 交互式 `run` 在选英雄阶段收到 EOF 时输出 `Aborted.` 且 `main()` 返回 130，不抛 `SystemExit` | `tests/unit/test_config.py::test_interactive_run_abort_returns_code_without_system_exit`；`rg -n "sys.exit\\(1\\)" src/ouro_agent/cli/main.py` 无输出 | P1 | Release | done |
| REQ-CLIUX-003 | G15/G16 | 安装版 help 不误导玩家去找源码 content | 所有带 `--content-dir` 的玩家命令 help 说明默认会优先使用当前 `./content`，否则使用 bundled installed content | `ouro play --help`、`ouro run --help`、`ouro doctor --help` 等不再出现 `defaults to ./content`，并包含 bundled installed content 文案 | `tests/unit/test_config.py::test_content_dir_help_describes_installed_fallback`；`/private/tmp/ouro_install_smoke_20260601/bin/ouro run --help` 输出 `bundled installed content` | P1 | Release | done |
| REQ-REL-001 | G15/G16/Engineering | GitHub 发布交接有可执行材料而不是口头说明 | 提供 tag-ready changelog、安装说明、clean venv smoke、发布前门禁、隐私检查和打 tag 步骤 | `CHANGELOG.md` 有 0.1.0 条目；`docs/engineering/RELEASE_HANDOFF_20260601.md` 说明 `pipx install ...@v0.1.0`、mock smoke、`share/ouro-agent/content`、门禁命令和 API key 隐私约束；README 链接交接材料 | `tests/unit/test_docs.py::test_release_handoff_covers_tag_install_smoke_and_privacy`；`CHANGELOG.md`；`docs/engineering/RELEASE_HANDOFF_20260601.md` | P1 | Release | done |
| REQ-AUDIT-001 | G19/G15/G16 | 最终目标何时能标完成有清晰依据 | 提供最终产品验收审计，把玩法、美术/TUI、数值、剧情、架构、发布、隐私和 QA 证据逐项映射，并列出尚不能标 complete 的风险 | 审计文档明确当前是 MVP release candidate 而非完整 goal complete；列出用户满意度、License、提交边界、真实 Provider smoke 等剩余风险 | `tests/unit/test_docs.py::test_final_product_audit_tracks_evidence_and_remaining_risks`；`docs/product/24_最终产品验收审计_20260601.md` | P1 | QA/Release | done |
| REQ-AUDIT-002 | G19/G15/G16 | 完整目标是否可标 complete 有一个机器汇总入口 | 新增 `scripts/completion_audit.py`，汇总 release gates、scope boundary 和 external sign-off，输出文本或 JSON 结论 | 默认运行 release check、release scope 和 signoff check；`--dry-run` 只展示将运行的命令；JSON 输出包含 `goal_complete_ready`、release/scope/signoff 状态和 pending keys；pending signoff 时不把 goal 判为 ready | `tests/unit/test_docs.py::test_completion_audit_reports_release_scope_and_signoff_without_marking_complete` 覆盖 dry-run、`--json --skip-release-check`、`goal_complete_ready=false`、scope ready 和 pending signoff keys；`venv312/bin/python scripts/completion_audit.py --dry-run`；`venv312/bin/python scripts/completion_audit.py --json --skip-release-check` 返回 3 并输出 `goal_complete_ready: false` | P1 | QA/Release | done |
| REQ-AUDIT-003 | G19/G15/G16 | 完整目标审计不只给 pending key，还给下一步动作 | `scripts/completion_audit.py` 把 `signoff_check.py --json` 的 pending items 汇总进 `signoff.pending_items`，文本输出列出每个 pending sign-off 的 action | JSON 输出包含 key/title/evidence/action/details；文本输出包含 `Required sign-offs:` 和用户满意度、License、live-provider、git-boundary 的下一步；不改变 pending 时返回 3 的语义 | `tests/unit/test_docs.py::test_completion_audit_reports_release_scope_and_signoff_without_marking_complete` 覆盖 JSON pending_items、stage-plan action 和文本 `Required sign-offs:`；`venv312/bin/python scripts/completion_audit.py --json --skip-release-check` 输出 pending item actions | P1 | QA/Release | done |
| REQ-DOCSYNC-001 | G15/G16/Engineering | 路线图状态不再和当前实现证据矛盾 | 更新 `docs/planning/OuroAgent_分步实现路线图_20260503.md`，把 S6/S6.1/S6.2/S6.3/S7/S8/S9 对齐到当前 release candidate 证据，并把下一步从继续实现旧阶段改为外部签收与发布边界 | 路线图顶部状态说明当前是 2026-06-01 release candidate；S6-S9 不再显示 partial/next/pending；明确完整 goal 仍受用户满意度、License、live-provider、git-boundary 签收约束 | `tests/unit/test_docs.py::test_final_product_audit_tracks_evidence_and_remaining_risks` 覆盖路线图 release candidate 状态、S6-S9 done 和外部签收下一步；`docs/planning/OuroAgent_分步实现路线图_20260503.md` | P1 | QA/Release | done |
| REQ-REL-002 | G15/G16/Engineering | 发布候选提交边界清楚，本地噪音不混入 tag | 提供 changeset manifest，说明 release candidate 应包含/排除的文件组；`.gitignore` 排除本地编辑器和虚拟环境目录 | `git status --short` 不再显示 `.obsidian/` 或 `venv312/`；manifest 明确 source/content/tests/docs/release files 属于交付范围，用户满意度和 License 仍为显式 sign-off | `tests/unit/test_docs.py::test_changeset_manifest_defines_release_boundary_and_local_excludes`；`docs/engineering/CHANGESET_MANIFEST_20260601.md`；`.gitignore` | P1 | Release | done |
| REQ-REL-014 | G15/G16/Engineering | 发布候选边界能被机器审计而不只靠人工看 git status | 新增 `scripts/release_scope.py`，按 changeset manifest 的 include/exclude 规则读取 `git status`，把变更分成 release-bound、local-only、unknown；release check 增加 scope-boundary gate | 当前 dirty tree 中 release-bound 变更可通过；出现 `.obsidian/`、venv、`.env`、build/dist 或未知顶层路径时返回非零；脚本不 stage、不 commit、不写文件 | `tests/unit/test_docs.py::test_release_scope_classifies_manifest_paths_and_rejects_local_noise` 覆盖 release-bound/local-only/unknown 分类和当前工作树 `--json` 为 ok；`tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 `--scope-only` 与 dry-run `scope-boundary`；`venv312/bin/python scripts/release_scope.py` 输出 `Scope OK`；`venv312/bin/python scripts/release_check.py --dry-run` 包含 `scope-boundary` | P1 | Release | done |
| REQ-REL-016 | G15/G16/Engineering | 发布候选 staging 有可审阅计划而不是手工从 dirty tree 里挑文件 | `scripts/release_scope.py --stage-plan` 在 release scope 通过时按根文件、content、docs、scripts、src、tests 分组打印 `git add -- ...` 命令；发现 local-only 或 unknown 路径时拒绝输出 staging plan；命令只读，不 stage、不 commit、不写文件 | 输出包含 `RELEASE STAGE PLAN`、release/local/unknown 计数、分组标题和可复制 `git add --` 命令；路径用 shell-safe quoting；运行前后 git index 不变 | `tests/unit/test_docs.py::test_release_scope_prints_read_only_stage_plan_without_mutating_index` 覆盖命令生成、空格路径 quoting、`--stage-plan` 输出和 git index 不变；`venv312/bin/python scripts/release_scope.py --stage-plan` 输出 `This script did not stage files.` 与分组 `git add --` 命令 | P1 | Release | done |
| REQ-REL-003 | G15/G16/Engineering | 发布前门禁能从仓库根目录一键复验 | 提供安全的 release check 脚本，串联 pytest、内容校验和 whitespace diff 检查，支持 dry-run 预览命令 | 脚本可从 repo root 运行，不包含产品逻辑、不访问网络、不写 secrets；失败时返回非零并指出失败 gate | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates`；`venv312/bin/python scripts/release_check.py --dry-run`；`venv312/bin/python scripts/release_check.py` 输出三项 release gates 通过 | P1 | Release | done |
| REQ-REL-004 | G15/G09/Engineering | 发布前能自动拦截疑似明文密钥 | release check 增加 privacy scan，扫描发布候选源码、内容、测试、示例、文档和脚本中的真实形态 token / bearer / secret 赋值 | 占位示例如 `sk-...` 不误报；疑似真实长密钥会返回非零并标出文件行号；扫描不访问网络、不读取本地虚拟环境和隐藏目录 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 dry-run、`--privacy-scan-only`、placeholder key 和真实形态 token；`venv312/bin/python scripts/release_check.py --privacy-scan-only` 输出 `Privacy scan OK: 247 release-bound text files checked.`；`venv312/bin/python scripts/release_check.py` 输出 `PASS: privacy-scan` | P1 | Release/Privacy | done |
| REQ-REL-005 | G15/G16/Engineering | release check 覆盖玩家最常用的安装诊断命令 | release check 增加隔离 HOME 的 `ouro doctor --lang en --content-dir content` gate，避免受本机真实 Provider 配置影响 | `scripts/release_check.py` 的 doctor gate 使用临时 `OURO_AGENT_HOME`，输出 `provider chk : READY`、content OK 和 mock play ready；dry-run 可预览该 gate | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 `--doctor-only` 与 dry-run；`venv312/bin/python scripts/release_check.py --doctor-only` 输出 `provider     : mock`、`provider chk : READY`、content OK、mock play ready；`venv312/bin/python scripts/release_check.py` 输出 `PASS: doctor` | P1 | Release | done |
| REQ-REL-006 | G15/G16 | 发布前 clean install smoke 可复验而不是只靠手工笔记 | release check 增加 `--install-smoke-only` 和 `--install-smoke`，创建临时 venv、`pip install .`，离开源码目录运行 `ouro --version`、`ouro doctor`、`ouro demo`、`ouro play --mock`、安装版 full run 和 `ouro status` | install smoke 使用临时 HOME 和临时 cwd，不依赖源码 `PYTHONPATH`；输出 version、doctor OK、guided demo、mock play victory/defeat 摘要、长期记录总览；失败时返回非零 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 `--install-smoke` dry-run 和 `--install-smoke-only` 文档；`venv312/bin/python scripts/release_check.py --install-smoke-only` 输出 `ouro 0.1.0`、content OK、`OURO DEMO :: FIRST ECHO`、`BATTLE COMPLETE`、`Result : victory`、`Run Summary`、`OURO STATUS :: ECHO LEDGER`、`Install smoke OK.` | P1 | Release | done |
| REQ-REL-007 | G15/G16/G18 | 安装版 smoke 覆盖完整 run 而不只是一场战斗 | install smoke 在 clean venv 安装后，从源码外运行 `ouro run --mock --auto --seed 7 --no-animation --no-trace`，并只打印结算摘要 | 输出 `Run Summary`、`Floor reached:`、`Run Archive:`，证明安装版 full run 能离开源码目录推进到结束并写本地归档 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 install smoke 执行 installed full run；`venv312/bin/python scripts/release_check.py --install-smoke-only` 输出 `YOU DIED`、`Run Summary`、`Floor reached: 4`、`Battles Won: 3`、`Battles Lost: 1`、`Run Archive:`、`Death History:`、`Install smoke OK.` | P1 | Release | done |
| REQ-REL-008 | G09/G16 | 安装版长期记录可回看 | install smoke 在安装版 full run 写入归档后，继续运行 `ouro runs --limit 1` 和 `ouro history --limit 1`，确认同一临时 HOME 能读回 run archive 与 death history | 输出 `RUN ARCHIVE :: ALL RUNS`、`DEATH HISTORY :: FALLEN RUNS`、`Seed: 7` run/death 摘要；证明安装版长期记录不是只写不读 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 install smoke 执行 installed `runs`/`history`；`venv312/bin/python scripts/release_check.py --install-smoke-only` 输出 `RUN ARCHIVE :: ALL RUNS`、`Seed: 7  Result: dead`、`DEATH HISTORY :: FALLEN RUNS`、`Seed: 7  Result: defeat`、`Install smoke OK.` | P1 | Release/Longview | done |
| REQ-REL-009 | G09/G16 | 安装版图鉴进度可回看 | install smoke 在安装版 quick battle 写入 Codex 后，继续运行 `ouro codex`，确认同一临时 HOME 能读回怪物图鉴进度 | 输出 `CODEX :: MONSTER ARCHIVE`、`Observed:` 与至少一个已解锁怪物条目；证明安装版 Codex 不是只写不读 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 install smoke 执行 installed `codex`；`venv312/bin/python scripts/release_check.py --install-smoke-only` 输出 `CODEX :: MONSTER ARCHIVE`、`Observed: 2/9`、`[FM] Hungry Cultist`、`[FM] Black Candle Acolyte`、`Install smoke OK.` | P1 | Release/Longview | done |
| REQ-REL-010 | G15/G16/Engineering | 发布版本号不漂移 | release check 增加版本一致性 gate，比较 `pyproject.toml`、`src/ouro_agent/__init__.py`、`CHANGELOG.md` 和 release handoff 的版本/推荐 tag | 版本号或 tag 不一致时返回非零；dry-run 可预览，单独 gate 可运行 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 dry-run 与 `--version-only`；`venv312/bin/python scripts/release_check.py --version-only` 输出 `Version consistency OK: 0.1.0 / v0.1.0`；`venv312/bin/python scripts/release_check.py --dry-run` 输出 `[DRY-RUN] version-consistency:` | P1 | Release | done |
| REQ-REL-011 | G15/G16/Engineering | 外部签收项不再散落在口头备注里 | 新增 `scripts/signoff_check.py` 和 release sign-off 文档，集中报告用户满意度、License、真实 Provider live smoke、Git 边界四个无法自动代替的签收项 | 默认模式动态读取 pending/ready 证据；`--json` 输出 machine-readable 报告；`--strict` 在 pending 时返回非零；不替用户选择 License、不伪造 live provider 证据 | `tests/unit/test_docs.py::test_signoff_check_reports_external_pending_items`、`test_signoff_check_detects_ready_markers_and_license_policy`；`venv312/bin/python scripts/signoff_check.py` 输出 `OURO RELEASE SIGN-OFF`、`Pending sign-offs: 4/4`；`venv312/bin/python scripts/signoff_check.py --json` 输出 `pending_count: 4`；`venv312/bin/python scripts/signoff_check.py --strict` 返回 3 | P1 | Release | done |
| REQ-REL-017 | G15/G16/Engineering | Git 边界签收失败时能直接看到下一步 staging 命令入口 | `scripts/signoff_check.py` 的 git-boundary pending 项读取 release scope 摘要，并在 action/detail 中提示 `venv312/bin/python scripts/release_scope.py --stage-plan`；JSON 输出同样包含该动作 | dirty tree pending 时输出 release-bound/local-only/unknown 计数和 stage-plan 命令；clean tree 时保持 READY；脚本仍不 stage、不 commit | `tests/unit/test_docs.py::test_signoff_check_reports_external_pending_items` 覆盖文本和 JSON 输出中的 stage-plan command、scope boundary summary、pending git-boundary；`venv312/bin/python scripts/signoff_check.py --json` 中 git-boundary action 包含 `scripts/release_scope.py --stage-plan` | P1 | Release | done |
| REQ-REL-018 | G15/G16/G19 | 用户满意度签收有可复制试玩路径而不是一句“请试玩” | `scripts/signoff_check.py` 的 satisfaction pending 项输出 3 条 acceptance command，覆盖 guided demo、固定 seed full run、completion audit；`completion_audit.py` 的 `signoff.pending_items` 继承这些 details | satisfaction pending 时文本和 JSON 都包含 `venv312/bin/ouro demo --lang en --seed 1 --content-dir content`、固定 seed mock run、`scripts/completion_audit.py --json`；不把这些命令等同于用户接受 | `tests/unit/test_docs.py::test_signoff_check_reports_external_pending_items` 覆盖 satisfaction details 和 JSON；`tests/unit/test_docs.py::test_completion_audit_reports_release_scope_and_signoff_without_marking_complete` 覆盖 pending_items 中的 acceptance commands；`venv312/bin/python scripts/signoff_check.py --json` 输出 satisfaction details | P1 | Release/QA | done |
| REQ-REL-019 | G15/G16/G19 | 用户验收试玩可以一条命令跑完并得到简洁报告 | 新增 `scripts/acceptance_check.py`，串联 guided demo、隔离 HOME 固定 seed full run、completion audit，并只输出关键通过标记；脚本明确不写 `SIGN-OFF: accepted` | 默认模式返回 0 表示验收命令都可运行，但仍不代表用户满意度已签收；`--json` 输出每步 returncode、missing markers、summary；`--dry-run` 只展示命令；不联网、不保存 API key、不污染默认用户档案 | `tests/unit/test_docs.py::test_acceptance_check_runs_review_commands_without_signing_off` 覆盖 dry-run、JSON、关键标记、completion audit pending 和不写签收；`venv312/bin/python scripts/acceptance_check.py --json` 输出 demo/full-run/completion-audit 均 ok 且 `marks_signoff: false` | P1 | Release/QA | done |
| REQ-REL-020 | G15/G16/G19 | 发布总门禁覆盖用户验收路径是否仍可跑通 | `scripts/release_check.py` 增加 acceptance-path gate，默认运行 `scripts/acceptance_check.py`；提供 `--acceptance-only` 和 `--skip-acceptance` 便于单独复验或跳过 | 默认 release check 包含 `PASS: acceptance-path`；`--dry-run` 显示 acceptance-path；`--acceptance-only` 输出 `OURO USER ACCEPTANCE CHECK` 和三步 PASS；该 gate 仍不写签收标记 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 `--acceptance-only`、dry-run acceptance-path 和文档入口；`venv312/bin/python scripts/release_check.py` 输出 `PASS: acceptance-path` | P1 | Release/QA | done |
| REQ-REL-012 | G15/G16/Engineering | 发布证据数字不会因测试或扫描范围变化而漂移 | release check 增加 evidence count gate，用 `pytest --collect-only -q` 统计测试数量，并用 release-bound privacy scan 文件枚举统计扫描数量；两者必须与 README、CHANGELOG、QA、审计、矩阵证据一致 | `--evidence-only` 单独可运行；默认 release check 包含 `evidence-count` gate；测试数量或 privacy scan 文件数变化但文档未同步时返回非零 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖 `--evidence-only` 和 dry-run；`venv312/bin/python scripts/release_check.py --evidence-only` 输出 `Evidence counts OK: 350 tests collected; 247 release-bound text files.`；`venv312/bin/python scripts/release_check.py --dry-run` 输出 `[DRY-RUN] evidence-count:` | P1 | Release | done |
| REQ-REL-013 | G15/G16/Engineering | 外部签收有可填写模板但不会被误判为通过 | 提供 `docs/engineering/USER_ACCEPTANCE_20260601.md` 和 `docs/engineering/PROVIDER_LIVE_SMOKE_20260601.md` pending 模板，列出试玩命令、验收项、live smoke 命令和签收行改写规则 | 模板默认 `SIGN-OFF: pending` 时 `signoff_check.py` 仍报告 pending；只有用户把满意度改为 `SIGN-OFF: accepted`、live smoke 改为 `SIGN-OFF: passed` 才 ready；模板不包含 API key 值 | `tests/unit/test_docs.py::test_signoff_templates_exist_but_do_not_fake_ready` 覆盖两个模板存在、包含 review/live-smoke 命令、默认 `SIGN-OFF: pending`、不记录 key，并验证 `signoff_check.py --json` 对 satisfaction/live-provider 仍为 pending；`venv312/bin/python scripts/signoff_check.py --json` 仍输出 `pending_count: 4` | P1 | Release | done |
| REQ-REL-015 | G15/G16/Engineering | License 决策有模板但不会被实现过程擅自决定 | 提供 `docs/engineering/LICENSE_DECISION_20260601.md` pending 模板，列出 MIT、Apache-2.0、私有发布等选项和需要同步的文件；`signoff_check.py` 在 license pending 时指向该模板 | 模板默认 `SIGN-OFF: pending` 时 license 仍 pending；只有用户选择策略并同步 `pyproject.toml`、README/README.zh，以及必要的 `LICENSE` 或私有发布政策后才 ready；模板不替用户选择 License | `tests/unit/test_docs.py::test_license_decision_template_guides_policy_without_marking_ready` 覆盖模板存在、MIT/Apache-2.0/private 选项、同步文件清单、README 链接和 `signoff_check.py --json` 中 license 仍为 pending；`venv312/bin/python scripts/signoff_check.py --json` 仍输出 license pending | P1 | Release | done |
| REQ-PROV-009 | G15/G09/Engineering | 真实 Provider 联通 smoke 有可执行且不泄密的路径 | 新增 `scripts/provider_smoke.py`：默认只离线 preflight；显式 `--live` 才运行真实 provider 战斗；live smoke 若中途降级 mock 则失败 | 未配置 key 时不联网且返回 NOT READY；env 存在时只显示 `set (hidden)`；dry-run 可预览 live 命令；live 模式用临时 `OURO_AGENT_HOME` 保存配置且只保存 env var 名 | `tests/unit/test_docs.py::test_release_check_script_documents_and_dry_runs_repo_root_gates` 覆盖脚本存在、缺 env NOT READY、env 存在时 `set (hidden)`、`--dry-run --live` 不联网；`venv312/bin/python scripts/provider_smoke.py --provider openai --model gpt-test --api-key-env OPENAI_API_KEY` 返回 3 并输出 `network : not called`、`status : NOT READY`；`env OURO_API_KEY=redacted-test-key venv312/bin/python scripts/provider_smoke.py --provider openai-compatible --model smoke-test --api-key-env OURO_API_KEY --base-url https://example.invalid` 输出 `env value : set (hidden)`、`status : READY` 且不回显 env 值 | P1 | Provider/Release | done |
| REQ-PROV-008 | G15/G09 | 真实 Provider 上场前有离线可验证预检 | 新增 `ouro config preflight`，在不发起网络请求、不显示 key 值的情况下检查 provider、model、base_url、api_key_env 和当前 shell 环境变量状态 | mock 显示无需 key 且返回 0；真实 provider 缺 env/model/base_url 时返回非零并给出可读问题；env 存在时只显示 `set (hidden)`，不回显值 | `tests/unit/test_config.py::test_config_preflight_redacts_env_value_and_reports_ready`、`test_config_preflight_missing_real_env_returns_not_ready`；`tests/unit/test_providers.py::test_provider_preflight_reports_missing_env_without_secret`、`test_provider_preflight_ready_for_configured_compatible_without_network`；`env OURO_AGENT_HOME=/private/tmp/ouro_preflight_ready_20260601 OURO_API_KEY=redacted-test-key venv312/bin/ouro --lang en config preflight` 输出 `env value    : set (hidden)`、`network      : not called`、`status       : READY`；`venv312/bin/python scripts/release_check.py` 输出 `350 passed` | P1 | Provider/Release | done |
| REQ-RUNSTATE-001 | G01/G04/Engineering | 路线、存档和恢复逻辑能可靠判断楼层是否结束 | `RunState.is_floor_complete(bundle)` 根据当前 floor 全部节点 visited 状态返回真实完成结果，不再硬编码 | 新 run 未完成；当前层所有节点 visited 后完成；进入下一层后重新变为未完成；多节点楼层缺一节点时仍未完成 | `tests/unit/test_run_state.py::test_run_state_reports_floor_completion_from_visited_nodes`；`rg -n "placeholder\\|not implemented" src/ouro_agent/sessions/run_state.py src/ouro_agent/cli/main.py` 无输出 | P1 | D/E | done |
| REQ-RUNSTATE-002 | G01/G14/G16 | 战斗屏能反映当前副本进度 | `run` 模式按 `RunState.current_floor` 生成战斗屏 floor label，不再在后续楼层沿用默认 `FLOOR 1` | 第 1 层显示 `EMBER CRYPT :: FLOOR 1`；推进到第 2 层显示 `EMBER CRYPT :: FLOOR 2`；中文显示 `灰烬墓室 :: 第 2 层`；固定 seed Boss 战输出第 4 层标题 | `tests/unit/test_run_state.py::test_run_battle_floor_label_follows_current_floor`；`env OURO_AGENT_HOME=/private/tmp/ouro_floor_audit_seed7 venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace --content-dir content` 的 Boss 战帧输出 `OURO AGENT :: EMBER CRYPT :: FLOOR 4` | P1 | D/E | done |

---

## 4. Slice 状态

| 切片 | 目标 | 状态 |
|------|------|------|
| Slice 0 | 可安装 CLI、Provider 配置、ASCII-safe 主屏骨架 | done |
| Slice A | 确定性战斗、action schema、mock、trace | done |
| i18n | 中英双语 UI / 内容 / Mock 旁白 | done |
| Provider 真实接入 | OpenAI / Anthropic / OpenAI-compatible adapter + 失败降级 | done |
| Slice B | 英雄、装备、词条、羁绊内容系统 | done |
| Slice C-Experience | 玩家可感知体验：主菜单、英雄/Prompt/Build 配置、实时动作帧、战报、数值指标 | done |
| Slice C-GameUI | CLI 游戏化体验：卡片、图标、背景、动作资产、Build 胡牌、图鉴迷雾、context 成长 | done |
| Slice C | CLI 观战表现（节奏、颜色、replay） | done |
| Slice D | 新手副本、路线、商店、奖励 | done |
| Slice E | 图鉴、死亡、长期保留 | done |
| Slice F | 批量试跑和基础平衡统计 | done |

## 4.1 Slice B 已交付的需求

| ID | 玩家价值 | 验收 | 证据 |
|----|---------|------|------|
| REQ-CONTENT-002 | 英雄 ≥ 3，可见辨识度 | 3 英雄、各 3 技能、各自默认 build | `ouro list-heroes` / `ouro hero-card <id>` 输出 |
| REQ-CONTENT-003 | 装备 / 词条 / 羁绊系统 | 6 装备 (3/2/1)、6 词条、2 羁绊；战前结算到属性 | `tests/unit/test_build.py` |
| REQ-DATA-003 | 装备/词条引用校验 | 悬空 item / affix 引用直接报错 | `test_dangling_item_reference_in_hero_fails` |
| REQ-DATA-004 | 传奇必填 allowed_effects | 缺失即拒绝加载 | `test_legendary_without_allowed_effects_rejected` |

## 4.2 Provider 真实接入已交付的需求

| ID | 玩家价值 | 验收 | 证据 |
|----|---------|------|------|
| REQ-PROV-004 | 真实模型可联通 | OpenAI / Anthropic / OpenAI-compatible adapter | `tests/unit/test_providers.py` 8 项 |
| REQ-PROV-005 | 兼容端点也可用 | base_url + api_key_env 自由组合，dashscope 桥实测 | smoke：dashscope curl 200 + 我方 adapter URL 一致 |
| REQ-PROV-006 | 失败不毁局 | 任意 ProviderError 后转 mock 完赛，玩家可见提示 | `test_fallback_provider_recovers_on_error` + 实跑日志 |
| REQ-PROV-007 | API key 永远只从环境变量取 | adapter 无 key 直接报清晰错 | `test_openai_provider_requires_env_var` |

## 4.3 下一阶段 P0/P1 需求

| ID | 来源 | 玩家价值 | 系统行为 | 验收标准 | 证据 | 优先级 | 切片 | 状态 |
|----|------|----------|----------|----------|------|--------|------|------|
| REQ-EXP-001 | G16/G01/G14 | 玩家一打开游戏就知道当前状态和下一步 | 主菜单显示 provider、model、mock/key 状态、language、visual mode，并把完整 run、Guided Demo、Quick Battle、Hero、Prompt、Status、Codex、History、Replay、Doctor、Config 作为入口 | mock 环境下新玩家不读 README 也能进入完整 `ouro run --mock` 或一条命令 `ouro demo`；长期进度和诊断入口不隐藏 | `tests/unit/test_config.py::test_cli_default_shows_main_menu`、`test_cli_accepts_global_language_after_subcommand`；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields`；`venv312/bin/ouro menu --lang en` 输出 `New Run        ouro run --mock`、`Guided Demo`、`Quick Battle`、`Status`、`Codex`、`History`、`Doctor` | P0 | C-Experience | done |
| REQ-MENUJOURNEY-001 | G22/G01/G14/G16 | 主菜单首屏能把试玩、配置、运行和复盘串成玩家旅程 | 主菜单新增 `PLAYER JOURNEY BOARD`，把 START/BUILD/RUN/LEARN 四步映射到 demo、list-heroes/prompt-templates、run、status/codex/run-report 命令 | 新玩家不读 README 也能理解从试玩到复盘的闭环；中英文核心界面宽度矩阵不溢出；原有入口列表保留 | `tests/unit/test_config.py::test_cli_default_shows_main_menu` 覆盖 journey board 四步和命令；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖中英文菜单宽度矩阵；`venv312/bin/ouro --lang en menu` 输出 `PLAYER JOURNEY BOARD` | P0 | C-Polish/E | done |
| REQ-RUNREADY-001 | G22/G03/G05/G16 | 进入副本前有最后一次战术确认，而不是直接进入战斗 | `render_run_setup_screen` 新增 `RUN READY BOARD`，展示 Prompt、Build 阶段、核心标签、下一轮 Build pick 和本地裁判规则 | 玩家在 run setup 里能确认 Agent 驾驶模式、构筑状态和首局打法；默认英文 ASCII-safe；原装备与 Prompt 契约保留 | `tests/unit/test_gameui.py::test_start_and_setup_screens_show_playable_entry_context` 覆盖 `RUN READY BOARD`、`[PROMPT] control`、`[BUILD] [ONLINE]`、`[CORE] shadow / control`、`[NEXT PICK]`、`[FIRST RULE] model chooses action, local judge resolves`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace --content-dir content` 输出 run ready board | P0 | C-Polish/E | done |
| REQ-ENCOUNTERBRIEF-001 | G20/G22/G06/G10/art08 | 进入第一回合前要先建立本战预期，而不是直接跳到 turn 1 | `play` 在 setup 后新增 `ENCOUNTER BRIEFING`，展示敌人构成、主威胁、Build 状态、反制窗口和开局计划 | 玩家开战前能知道敌人是谁、哪个 ATB/吟唱风险最高、当前 Build 应如何开局以及是否需要预留打断；默认英文 ASCII-safe | `tests/integration/test_mock_battle.py::test_cli_play_prints_turn_frames_and_report` 覆盖 `ENCOUNTER BRIEFING`、`[ENEMY]`、`[THREAT]`、`[WINDOW]`、`[PLAN]`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 encounter briefing | P0 | C-Polish/E | done |
| REQ-EXP-002 | G16/G03/G14 | 玩家能理解英雄、武器、Build、技能和 Prompt 倾向 | 英雄选择/详情显示短标识、武器、Build、技能 MP/CD、策略摘要和上手难度 | 6 英雄页面都能说明“怎么玩”和“风险是什么” | `tests/unit/test_build.py::test_render_all_six_hero_cards_explain_play_and_risk` / `ouro list-heroes`、`ouro hero-card` 输出；6 英雄均已完成 | P0 | C-Experience | done |
| REQ-HEROROSTER-001 | G22/G03/G05/G16 | 英雄列表能先比较开局驾驶方案，而不是只给短卡片 | `list-heroes` 新增 `HERO ROSTER BOARD`，按英雄展示推荐 Prompt、Build 阶段、风险、核心标签、开局动作和 hero-card 下一步命令；英雄选择卡使用 `[HERO]` 图形语义 | 6 英雄选择前可横向比较；默认英文 ASCII-safe；hero select card 不误用 `[PATH]` 路线图标 | `tests/unit/test_build.py::test_render_hero_list_en_is_ascii_safe` 覆盖 `HERO ROSTER BOARD`、Astia/Norn/Vela 推荐 Prompt、核心标签、`[HERO] ##@##` 图形主体、无 `[PATH] ##==>## route` 和 `ouro hero-card <hero_id> --prompt-style <name>`；`venv312/bin/ouro --lang en list-heroes --content-dir content` 输出 roster board | P0 | C-Polish/E | done |
| REQ-EXP-003 | G16/G05 | 玩家能用半自动方式配置 Agent 倾向 | 提供 Aggressive/Guarded/Control/Attrition 等 Prompt 模板和策略槽 | 选择模板后下一场 prompt/hero-card/trace 可见变化 | `tests/integration/test_mock_battle.py::test_cli_prompt_style_is_visible_in_trace` / `ouro prompt-templates`、`ouro play --prompt-style control` trace | P0 | C-Experience | done |
| REQ-PROMPTPILOT-001 | G22/G05/G16 | Prompt 模板页能像 AI 驾驶模式选择器，而不是纯文字说明 | `prompt-templates` 新增 `PROMPT PILOT BOARD`，用 BURST/STABLE/DENY/GRIND 展示四种模板的用途、风险、适配 Build 和 run 命令 | 玩家能直接比较 aggressive/guarded/control/attrition 的战术定位；输出保持 ASCII-safe；模板说明仍保留 | `tests/unit/test_config.py::test_cli_prompt_templates_show_pilot_board` 覆盖 `PROMPT PILOT BOARD`、四种驾驶标签和 `ouro run --mock --prompt-style <name>`；`venv312/bin/ouro --lang en prompt-templates` 输出 pilot board | P0 | C-Polish/E | done |
| REQ-PROMPTSCENARIO-001 | G22/G05/G16/G20 | Prompt 选择要对应具体战况，而不是只看性格标签 | `prompt-templates` 新增 `PROMPT SCENARIO BOARD`，按 CHANT、LOW HP、EXECUTE、BOSS 四类战况展示推荐驾驶模式和行为偏置 | 玩家能根据当前失败/目标选择 control、guarded、aggressive 或 attrition；Boss/吟唱、低血、收割场景都有明确推荐；原模板说明保留 | `tests/unit/test_config.py::test_cli_prompt_templates_show_pilot_board` 覆盖 `PROMPT SCENARIO BOARD`、`[CHANT] control -> interrupt high ATB`、`[LOW HP] guarded`、`[EXECUTE] aggressive`、`[BOSS] control` 和 `PICK` 推荐；`venv312/bin/ouro --lang en prompt-templates` 输出 scenario board | P0 | C-Polish/E | done |
| REQ-EXP-004 | G16/G06/G14 | 自动战斗像游戏而不是最终报告 | 每个 hero/enemy turn 后输出或刷新 Battle Frame | `play --mock --delay 0.1` 能看到逐 turn 动作、目标、影响、日志 | `tests/integration/test_mock_battle.py::test_cli_play_prints_turn_frames_and_report` / `venv312/bin/ouro play --mock --seed 1` 输出 | P0 | C-Experience | done |
| REQ-EXP-005 | G16/G06/G09/G10 | 玩家能理解战斗为什么赢或输 | 战斗结束输出战报：行动次数、技能使用率、普攻占比、主要伤害来源、死亡原因 | 胜利/失败各有固定 seed 示例战报 | `tests/integration/test_mock_battle.py::test_battle_report_summarizes_action_mix` / `venv312/bin/ouro play --mock --seed 1` 输出 | P0 | C-Experience | done |
| REQ-BATTLERESULT-001 | G22/G09/G10/G16 | 战斗结束先给可扫读结果板，再进入详细诊断 | `render_battle_report()` 新增 `BATTLE RESULT BOARD`，汇总结果、HP/MP、hero/enemy turn、tick、basic:skill、fallbacks、伤害压力和下一步建议 | 玩家不读长诊断也能知道这局是稳定胜、险胜、失败还是超时，以及下一局该保守、提速或换 Prompt；原 Tactical Diagnosis/Failure Review 保留 | `tests/integration/test_mock_battle.py::test_battle_report_summarizes_action_mix` 覆盖 `BATTLE RESULT BOARD`、`[RESULT] victory`、`[TEMPO] hero`、`[ACTION] basic:skill`、`[DAMAGE] dealt`、`[NEXT]`；`venv312/bin/ouro --lang en play --mock --seed 1 --no-animation --no-trace --content-dir content` 输出 battle result board | P0 | C-Polish/E | done |
| REQ-BATTLETURNMAP-001 | G22/G09/G10/G16/G20 | 战后复盘要先看到整场回合节奏，而不是只读统计和长诊断 | `render_battle_report()` 在结果板后新增 `BATTLE TURN MAP`，用 `Hxxx`/`Exxx` token 展示英雄/敌方行动轨道，并汇总首个英雄行动、峰值命中、敌方伤害和复盘读法 | 玩家和策划能快速判断英雄/敌方节奏、关键伤害点、敌方爆发和隐藏长尾；原详细统计、Tactical Diagnosis 和状态详情保留 | `tests/integration/test_mock_battle.py::test_battle_report_summarizes_action_mix` 覆盖 `BATTLE TURN MAP`、`[FLOW] H`、`[FIRST HERO]`、`[IMPACT] peak hit`、`[READ]`；`venv312/bin/ouro --lang en play --mock --seed 1 --no-animation --no-trace --content-dir content` 输出 battle turn map | P0 | C-Polish/E | done |
| REQ-EXP-006 | G16/G04 | 默认试玩有玩家选择而非静默自动流 | 路线、奖励、商店、休息显示 CLI 选择项；`--auto` 才自动 | 默认 run 中至少出现一次输入选择；batch 模式标记 auto | `venv312/bin/ouro run --mock --auto` 输出 / 人工试玩 | P0 | D | done |
| REQ-EXP-007 | G16/G10 | 数值调优不只看胜率 | 批量试跑输出 hero/build/tier、技能使用率、basic_attack 占比、ready skill ignored、死亡来源 | 50 局 mock 报告包含上述字段 | `tests/unit/test_battle.py::test_batch_run_is_reproducible_with_same_seed` / `ouro batch --mock --count 50` 报告 | P0 | F | done |
| REQ-BATCHTUNE-001 | G22/G10/G18/G19 | 批量试跑报告能先给策划调参判断，而不是只堆指标 | `BatchResult.summary()` 新增 `BALANCE TUNING BOARD`，汇总胜率、节奏异常、技能/普攻比例、资源循环和调参建议 | 策划打开 batch 报告即可判断样本是否稳定、是否需先查 outlier、是否要调输出/生存/MP；原统计字段保留 | `tests/unit/test_battle.py::test_batch_run_produces_reproducible_results` 覆盖 `BALANCE TUNING BOARD`、`[WIN]`、`[TEMPO]`、`[ACTION]`、`[RESOURCE]`、`[TUNE]`；`venv312/bin/ouro --lang en batch --hero hero_shadow_apprentice --enemies enemy_hungry_cultist enemy_black_candle_acolyte --count 3 --seed 1 --quiet` 输出 balance tuning board | P0 | C-Polish/F | done |
| REQ-BATCHHEAT-001 | G22/G10/G18/G19 | 批量试跑要能一眼看出每局样本的稳定性和异常点 | `BatchResult.summary()` 新增 `BATCH SAMPLE HEATMAP`，用 V/D/T、英雄回合数和异常 token 展示前 12 局样本，并给出策划读法 | 策划不用阅读长统计也能发现胜负波动、超预算局、MP 枯竭、破防错失、反制错失和防御循环；原统计字段保留 | `tests/unit/test_battle.py::test_batch_run_produces_reproducible_results` 覆盖 `BATCH SAMPLE HEATMAP`、`[KEY] V victory`、`[RESULT]`、`hero turns / budget`、`[ALERT]`、`[READ]`；`venv312/bin/ouro --lang en batch --hero hero_shadow_apprentice --enemies enemy_hungry_cultist enemy_black_candle_acolyte --count 3 --seed 1 --quiet` 输出 sample heatmap | P0 | C-Polish/F | done |
| REQ-EXP-008 | G16/G15/G09 | 新玩家和发布评审能一条命令看到完整体验闭环 | 新增 `ouro demo`，强制 mock、无 trace、无动画，串联状态菜单、英雄卡、确定性战斗和 Codex 读回 | 无网络、无 API key、无交互即可输出首屏体验；包含 `Guided Demo` 菜单入口、英雄 Build、`BATTLE COMPLETE`、Codex readback；不写 trace | `tests/unit/test_config.py::test_cli_demo_runs_guided_mock_smoke`；`tests/unit/test_config.py::test_content_dir_help_describes_installed_fallback` 覆盖 `demo --help`；`venv312/bin/ouro demo --lang en --seed 1 --content-dir content` 输出 `OURO DEMO :: FIRST ECHO`、`STEP 3: Deterministic mock battle`、`BATTLE COMPLETE`、`CODEX :: MONSTER ARCHIVE` 且不输出 `Trace :` | P1 | C-Experience/Release | done |
| REQ-EXP-009 | G16/G15 | Demo 结束后玩家知道下一步该做什么 | `ouro demo` 在 Codex 读回后输出下一步命令出口，指向完整 run、Status、Codex、Runs 和 Doctor | 新玩家看完 demo 不需要回 README 也能进入正式 run 或查看长期记录；输出保持 ASCII-safe/中英双语 | `tests/unit/test_config.py::test_cli_demo_runs_guided_mock_smoke` 覆盖 `STEP 5: Continue from here`、`Next commands`、`ouro run --mock`、`ouro status --lang en`、`ouro codex --lang en`、`ouro runs --lang en --limit 5`、`ouro doctor --lang en`；`venv312/bin/ouro demo --lang en --seed 1 --content-dir content` 输出下一步命令出口 | P1 | C-Experience | done |
| REQ-GAMEUI-001 | G17/G14 | CLI 像游戏界面而不是纯文本日志 | 实现卡片、图标、低密度场景背景的 ASCII-safe/Unicode 规格 | 主菜单、英雄、奖励、图鉴至少 4 类卡片有快照 | `render_main_menu`、`render_hero_card`、`render_reward_choice`、`render_codex_card` / `tests/unit/test_codex.py` | P0 | C-GameUI | done |
| REQ-GAMEUI-002 | G17/G13/art02/art03 | 角色和怪物有动作与状态 | 每个英雄和 MVP 怪物有 idle/cast/attack/hit/low/death/victory 等动作状态 | 战斗帧能根据动作类型显示不同姿态/文案 | `tests/unit/test_gameui.py::test_hero_sprite_changes_with_pose` / `ouro play --mock` 输出 | P0 | C-GameUI | done |
| REQ-GAMEUI-003 | G17/G07/G14 | 图鉴是卡片收藏而不是文字列表 | 图鉴支持 locked/fog/observed/mastered 卡片状态 | 未解锁字段以迷雾/占位显示，已解锁字段可注入 Prompt | `tests/unit/test_codex.py` 14 项测试（render_codex_card、render_codex_summary、CodexStage、CodexProgress）/ 图鉴渲染输出 | P0 | C-GameUI | done |
| REQ-CODEXPLAN-001 | G17/G07/G14/G16 | 图鉴卡能直接指导下一次战斗反制 | 怪物详情卡新增 `COUNTER PLAN BOARD`，按 Codex 阶段逐步展示威胁、窗口、Build 反制和猎杀循环；未知状态保持迷雾 | Unknown 只显示反制迷雾；Observed 显示威胁但不泄露 Build；Familiar 显示窗口；Mastered 显示 Build 标签；Hunted 显示循环打法；默认宽度不溢出 | `tests/unit/test_codex.py::test_codex_card_unknown`、`test_codex_card_observed`、`test_codex_card_familiar`、`test_codex_card_mastered`、`test_codex_card_hunted`；`venv312/bin/ouro codex enemy_black_candle_acolyte --lang en --content-dir content` 输出 `COUNTER PLAN BOARD` | P0 | C-Polish/E | done |
| REQ-CODEXHUNT-001 | G17/G07/G14/G16 | 图鉴总览能指导下一局研究目标 | `render_codex_summary` 新增 `CODEX HUNT BOARD`，汇总 unknown/familiar/mastered 缺口、未掌握威胁池、优先狩猎目标和详情命令 | 空图鉴、部分观察和家族旧存档都能看到下一步目标；优先目标不默认跳到 Boss；输出 80 列内稳定 | `tests/unit/test_codex.py::test_codex_summary`、`test_codex_summary_counts_family_tier_progress`、`test_codex_summary_no_progress` 覆盖 hunt board、缺口、威胁池、优先目标、下一步命令和宽度；`venv312/bin/ouro codex --lang en --content-dir content` 输出 `CODEX HUNT BOARD` | P0 | C-Polish/E | done |
| REQ-BUILDJOY-001 | G17/G03/G10 | 玩家有凑 Build 和胡牌的快感 | 实现 Build Seed/Pair/Online/High Roll/Locked In 阶段、active/near/best next picks | 拿奖励和触发羁绊时有 Build event 和"差几张"提示 | `tests/unit/test_build.py::test_build_progress_calculates_stage_for_astia`、`tests/unit/test_build.py::test_build_progress_provides_best_next_picks`、`tests/unit/test_gameui.py::test_dynamic_next_pick_in_battle_screen` / `ouro play --mock` 输出 | P0 | C-GameUI | done |
| REQ-CONTEXT-001 | G17/G07/G05 | 经验能调整 Agent context 窗口 | 实现 Strategy/Codex/Memory/Prompt edit 槽位展示和升级选择 | 升级后下一场 BattleLLMSession 的 context 注入变化可见 | `ContextProgress`、`render_context_window` / `tests/unit/test_context.py` | P0 | C-GameUI | done |
| REQ-CONTEXTGROWTH-001 | G22/G07/G05/G16 | Context 成长要像 Agent 能力面板，而不是只列数字槽位 | `render_context_window()` 新增 `CONTEXT GROWTH BOARD`，展示等级进度、下一奖励、Strategy/Codex/Memory/Prompt/Build 五类槽位的用途 | 玩家能理解升级分别影响驾驶策略、怪物知识注入、长期记忆、Prompt 修正和 Build 提示；Build Hint 槽位进入 UI；中英文固定 56 列不溢出 | `tests/unit/test_context.py::TestRenderContextWindow::test_render_context_default` 覆盖中文 `CONTEXT GROWTH BOARD`、`[LEVEL]`、`[STRATEGY]`、`[CODEX]`、`[MEMORY]`、`[PROMPT]`、`[BUILD]`、`Build 提示: 1/2` 和 56 列宽度；`test_render_context_en` 覆盖英文 `Build Hint: 1/2` 与 growth board | P0 | C-Polish/E | done |
| REQ-STORY-001 | G17/G13 | 副本有短剧情和场景氛围 | 每个副本有场景句、节点事件、怪物预兆、Boss 句、图鉴碎片 | 新手副本战前/路线/Boss/图鉴能显示短叙事 | `tests/unit/test_gameui.py::test_story_surfaces_show_route_boss_battle_and_codex_fragments` 覆盖路线预兆、Boss omen、战斗场景句和 Codex 碎片；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 验证长叙事在 80/100/120 与中英文核心界面下无溢出；CLI run 节点进入阶段继续通过 `_narrative_block()` 输出 encounter/boss 文案 | P1 | C-GameUI | done |
| REQ-GAMEUI-004 | G17/G14/art08 | 战斗有左右对战画面而不是文字列表 | 默认战斗主屏左英雄右怪物，中间显示技能弹道/命中/格挡效果层 | 100 列和 80 列快照都能看到左右站位、精灵帧、HUD 和短日志 | `tests/integration/test_mock_battle.py::test_battle_screen_uses_duel_layout_build_and_session`、`test_battle_screen_has_80_column_compact_duel_snapshot` / `ouro play --mock --no-animation` 输出 | P0 | C-GameUI | done |
| REQ-ART-002 | art02/art03/art08 | 角色、怪物、武器和 Build 有可识别图形语言 | 英雄/怪物交付 4-6 行战斗精灵，武器用 `[W:*]` 图标，Build 用阶段徽章 | 每个 MVP 英雄和怪物至少有 idle/cast/attack/hit/death 或等价帧 | `tests/unit/test_gameui.py::test_weapon_icon_in_battle_screen` / `ouro play --mock` 输出 | P0 | C-GameUI | done |
| REQ-SESSION-001 | G05 v0.2 | 真实模型战斗更连贯且少重复上下文 | 每场战斗创建唯一 `BattleLLMSession` | trace 有 `battle_session_id`，同一场战斗内复用 | `tests/integration/test_mock_battle.py::test_battle_llm_session_ids_static_hash_and_delta_are_traced` / `/tmp/ouro_trace_check/*.trace.jsonl` | P0 | C0 | done |
| REQ-SESSION-002 | G05 v0.2 | 减少重复输入和 token 成本 | Prompt Composer 拆分 static context 与 turn delta | 静态 Build/技能/图鉴只初始化一次，回合只传 delta | `.venv/bin/python -m pytest -q` / prompt serializer tests | P0 | C0 | done |
| REQ-SESSION-003 | G05/G09 | 模型决策可复盘 | trace 记录 `static_context_hash`、`delta_context`、session reset | trace 可定位每回合用到的上下文 | `/tmp/ouro_trace_check/*.trace.jsonl` contains `static_context_hash`, `delta_context_id`, `delta_context` | P0 | C0 | done |
| REQ-BUILD-001 | G03 v0.3 | Build 真正影响打法 | 内容数据支持 build_archetype、priority、avoid | hero-card 和 prompt 能展示不同 Build 策略 | `tests/unit/test_build.py::test_render_hero_card_shows_build_strategy_and_prompt_template` / `ouro hero-card` 输出 | P0 | C1 | done |
| REQ-BUILD-002 | G03 v0.3 | 装备和词条影响武器/技能选择 | 装备、词条、羁绊转译为模型策略提示 | 更换装备后 Build plan 变化 | `tests/unit/test_build.py::test_build_progress_calculates_stage_for_astia` / `ouro hero-card` 输出 | P0 | C1 | done |
| REQ-STATUS-001 | G03/G14/art04 | 玩家看懂 Buff/Debuff | 状态分成 Buff 和 Debuff，统一短名与描述 | 战斗主屏显示 `BUFF` / `DEBUFF` 分组 | `tests/integration/test_mock_battle.py::test_battle_screen_uses_duel_layout_build_and_session` | P0 | C1 | done |
| REQ-UI-001 | G14/art07 | 英雄选择更像游戏 | 英雄详情显示头像、Build、装备、标签、羁绊、策略摘要 | `ouro hero-card` 显示 Build 面板 | `tests/unit/test_build.py::test_hero_card_shows_build_stage_badge` / `ouro hero-card` 输出 | P0 | C1 | done |
| REQ-UI-002 | G14/art07 | 模型 Session 信息可见但不喧宾夺主 | 战斗主屏展示 Battle Echo、Sealed Echo、Echo Cost、Ritual Time | Session 面板低于 HP/MP/ATB 和行动结果 | `tests/integration/test_mock_battle.py::test_battle_screen_uses_duel_layout_build_and_session` | P1 | C1 | done |
| REQ-CONTENT-004 | G03/G13/art02 | 英雄选择更丰富 | 内容规划扩展到 6 英雄，新增 Mirel/Korr/Sera | 6 英雄均有 ID、短标识、职业、Build 倾向 | `tests/unit/test_build.py::test_six_heroes_loaded`、`tests/unit/test_build.py::test_six_heroes_have_identity_and_build_tendency`；`venv312/bin/ouro validate-content` 显示英雄 6 | P1 | C2 | done |
| REQ-MON-001 | G07/G13/G10 | 怪物不再只是加血小怪 | 怪物按 family + tier 三档建模 | 至少 4 家族 I/II 档，1 个 III 档 Boss | `tests/unit/test_build.py::test_monster_families_have_tier_ladders_and_boss_mock_battle`；`venv312/bin/ouro validate-content` 显示敌人 9；mock boss battle 可运行到胜/负/超时结算 | P1 | C2 | done |
| REQ-CODEX-001 | G07/G14/art07 | 图鉴能影响模型决策 | 图鉴按 family + tier 分阶段解锁，并限制模型可见信息 | 未解锁信息不进 prompt，已解锁信息显示在 Codex | `tests/unit/test_codex.py::test_codex_progress_tracks_family_tier_independently`、`tests/unit/test_codex.py::test_prompt_codex_visibility_is_stage_and_tier_limited`、`tests/unit/test_codex.py::test_codex_summary_counts_family_tier_progress`；`tests/unit/test_codex.py` 全绿 | P1 | C2 | done |
| REQ-PLAY-001 | G18/G04/G16 | 路线选择像肉鸽决策而不是列表 | 路线卡显示风险、预期消耗、收益、Build 适配和侦察信息 | 玩家在进入节点前能判断为什么选这条路；`--auto` 输出也保留选择理由 | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出包含 `Cost:`、`Reward:`、`Build fit:`、`Scout:`、`Decision:` | P0 | D | done |
| REQ-PLAY-002 | G18/G03/G17 | 奖励选择有 Build 胡牌反馈 | 奖励卡显示属性、标签、Build 阶段前后变化和下一战策略影响 | 选择奖励前能看到 `[ONLINE] -> [HIGH]` 或标签缺口变化等结果，并能识别跨 Build 种子 | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出包含 `Build before/after`、`Stat delta`、`Tag delta`、`AI impact`、`Cross-build seed` | P0 | D | done |
| REQ-PLAY-003 | G18/G04/G05 | 商店和休息能修正本局策略 | 商店提供恢复、定向标签、Prompt 风格调整、侦察；休息提供 Recover/Focus/Study 互斥选项 | 玩家能通过非战斗节点修复 HP/MP、补 Build 缺口或改变 Prompt 倾向 | `tests/unit/test_run_state.py::test_shop_repairs_resources_strategy_and_scouting`、`tests/unit/test_run_state.py::test_rest_options_are_mutually_exclusive_strategy_repairs`、`tests/integration/test_mock_battle.py::test_shop_strategy_change_is_visible_in_next_battle_trace`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 商店/休息输出 | P1 | D | done |
| REQ-PLAY-004 | G18/G06/G10 | 失败让玩家知道下一局怎么改 | 战报从 trace 归因资源、目标、Build、路线问题并输出 1-3 条建议 | 固定失败 seed 的战报能指出 MP 枯竭、未打断、输出不足或路线风险 | `tests/unit/test_battle.py::test_battle_report_includes_trace_based_failure_review`、`tests/integration/test_mock_battle.py::test_battle_report_shows_trace_based_defeat_lessons`；`venv312/bin/ouro --lang en batch --hero hero_shadow_apprentice --enemies enemy_black_candle_high_priest_archive --count 3 --seed 1 --quiet` | P0 | F | done |
| REQ-PLAY-005 | G18/G05 | Prompt 模板真正改变打法 | Aggressive/Guarded/Control/Attrition 影响收割、打断、防御、MP 保留阈值 | 同一 seed 切换模板后行动统计或关键行动有可解释差异，战报显示本场模板带来的关键行为 | `tests/unit/test_providers.py::test_mock_prompt_style_changes_tactical_choice`、`tests/unit/test_providers.py::test_mock_guarded_style_raises_defensive_threshold`、`tests/unit/test_providers.py::test_mock_attrition_style_changes_target_and_dot_priority`、`tests/unit/test_battle.py::test_battle_report_includes_prompt_impact_for_style`、`tests/integration/test_mock_battle.py::test_battle_report_shows_prompt_impact`；`venv312/bin/ouro --lang en batch --hero hero_shadow_apprentice --enemies enemy_hungry_cultist enemy_black_candle_acolyte --count 5 --seed 7 --prompt-style control --quiet` | P1 | F | done |
| REQ-BAL-001 | G10 v0.2 | 能判断英雄/Build/怪物档次是否合理 | 批量试跑统计按 hero/build/tier 输出 | 至少 50 局 mock seed 集合有统计 | `tests/unit/test_battle.py::test_batch_run_produces_reproducible_results` 覆盖 `Build:`、`Enemy tiers:`、`Group key:`；`venv312/bin/ouro --lang en batch --count 50 --seed 1 --quiet` 输出 `Build: Black Candle Interrupt [ONLINE]`、`Enemy tiers: trace=2`、`Total runs: 50` | P2 | F | done |
| REQ-BAL-002 | G18/G10 | 平衡不只看胜率，也看是否拖沓 | 批量试跑新增战斗时长分布、超时原因、MP 枯竭、输出不足、防御循环指标 | 50 局报告能定位诺恩式防御循环和 Boss 超时原因 | `tests/unit/test_battle.py::test_batch_report_includes_length_distribution_and_timeout_causes`、`tests/integration/test_mock_battle.py::test_cli_batch_reports_timeout_causes`；`venv312/bin/ouro --lang en batch --hero hero_ash_guardian --enemies enemy_black_candle_high_priest_archive --count 50 --seed 100 --quiet`；`venv312/bin/ouro --lang en batch --hero hero_shadow_apprentice --enemies enemy_black_candle_high_priest_archive --count 2 --seed 1 --max-ticks 20 --quiet` | P1 | F | done |
| REQ-QOL-001 | G18/G15 | 新玩家少踩 CLI 参数顺序坑 | 对全局参数放在子命令后的情况提供兼容或更清楚错误提示 | `ouro menu --lang zh` 能工作，或错误提示明确说应写 `ouro --lang zh menu` | `tests/unit/test_config.py::test_cli_accepts_global_language_after_subcommand`; `venv312/bin/ouro menu --lang en` 输出 `Language : en` | P2 | C-Experience | done |
| REQ-UIQA-001 | G19/G14/art06 | 所有核心界面排布稳定正常 | 建立 80/100/120 宽度视觉快照和无溢出验收 | 主菜单、英雄、路线、奖励、商店、休息、状态总览含下一局计划、战斗、战报在中文/英文下无边框错位和关键字段丢失 | `tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖 80/100/120 与中英文核心字段；`tests/unit/test_gameui.py` 全绿；`git diff --check` | P0 | C-Polish | done |
| REQ-ART-003 | G19/G14/art06 | 界面有统一高级感而不是各页拼凑 | 统一卡片、面板、图标、状态短名和布局层级 | 所有可选择对象使用同一套卡片规则；状态主屏显示短名，详情保留完整 ID | `tests/unit/test_gameui.py::test_selectable_surfaces_share_card_rules_and_status_details` 覆盖英雄列表/路线/奖励/商店/休息 choice card 规则与主屏短状态、战报完整 status id；`tests/unit/test_tui_frame_builder.py::test_pixel_skin_panels_are_ascii_and_fixed_width` 覆盖中英文 pixel panel 宽度；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` | P0 | C-Polish | done |
| REQ-BATTLEUI-001 | G19/art08 | 战斗主屏第一眼能看懂行动结果 | 重排战斗主屏，突出 ACTION，压缩 Build/Session/Log，修复长行挤压 | 阿斯缇娅 seed 7 第 3 战 80/100 列下行动、目标、裁判、HP/MP/ATB 均可见 | `tests/unit/test_tui_frame_builder.py::test_seed7_third_battle_action_result_is_readable` 覆盖 80/100 列 ACTION/目标/Judge/HP/MP/ATB/Counter；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 固定 seed 战斗输出 | P0 | C-Polish | done |
| REQ-BOSSUI-001 | G19/G10/art08 | Boss 战像特殊战斗而不是厚血怪 | Boss 主屏显示 Phase/Charge/Break/Enrage，并输出错过反制窗口 | 诺恩 seed 7 Boss 战不再无解释 600 ticks 超时；失败也能知道错过窗口 | `tests/unit/test_tui_frame_builder.py::test_boss_frame_exposes_phase_charge_break_enrage`、`tests/unit/test_tui_frame_builder.py::test_boss_battle_screen_shows_special_intel_without_width_drift`、`tests/unit/test_tui_frame_builder.py::test_tower_brace_counter_renders_boss_break`、`tests/integration/test_mock_battle.py::test_norn_seed7_boss_batch_is_explained_and_within_budget`；batch 输出含 `Budget: boss 8-18 hero turns`、`Timeouts: 0`、`Counter windows: 5/5 answered, 0 missed` | P1 | D/F | done |
| REQ-BAL-003 | G19/G10 | 数值调整有节奏预算和异常原因 | 落地普通/精英/Boss 行动数预算，记录 tempo_outlier、mp_dry_turns、defense_loop_turns | batch 报告能区分 MP 枯竭、输出不足、防御循环、Boss 破防错过 | `tests/unit/test_battle.py::test_batch_tempo_metrics_distinguish_resource_loop_and_boss_break` 覆盖 normal/elite/boss 预算、`mp_drought`、`defense_loop`、`boss_break_missed` 与指标键；`venv312/bin/ouro --lang en batch --count 50 --seed 100 --quiet` 输出 `Budget: normal 3-8 hero turns` 与 `Metric keys: tempo_outlier=0, mp_dry_turns=0, defense_loop_turns=0, boss_break_missed=0`；`venv312/bin/ouro --lang en batch --hero hero_shadow_apprentice --enemies enemy_black_candle_high_priest_archive --count 3 --seed 7 --quiet` 输出 `boss_break_missed=10` 与 `Outlier reasons: boss_break_missed: 3` | P0 | F | done |
| REQ-QA-001 | G19/G05/G09 | 每次界面和数值改动都有证据 | 建立模块级视觉回归、固定 seed 试玩和人工验收记录模板 | 每个 done 需求都有测试、快照、trace、命令输出或试玩记录 | `docs/product/23_QA证据与固定Seed试玩记录_20260601.md` 记录证据门禁、固定 seed 试玩命令、视觉回归范围和人工试玩模板；`tests/unit/test_requirement_matrix.py::test_done_requirements_have_replayable_evidence` 扫描全部 done 行证据；`tests/unit/test_requirement_matrix.py::test_qa_note_records_fixed_seed_evidence_and_manual_template` 校验 QA 记录包含命令证据与人工模板 | P0 | QA | done |
| REQ-MANUAL-001 | G19/G16 | 最终体验不只靠自动测试，还要有人工试玩观察 | 记录一次固定 seed 完整 run 人工试玩，覆盖路线、奖励、商店、休息、普通战、Boss、失败复盘、Run Archive 和 Death History | 试玩记录包含日期、命令、seed、语言、观察重点、实际结果、证据位置、结论和后续问题；命令输出可复验 | `tests/unit/test_docs.py::test_manual_playtest_note_records_full_run_observations`；`docs/product/25_人工试玩记录_20260601.md`；`/private/tmp/ouro_manual_playtest_seed7_20260601.log` 输出 `YOU DIED`、`Floor reached: 4`、`Run Archive:`、`Death History:` | P1 | QA/Release | done |
| REQ-DLG-001 | G20/G13/G14 | 英雄像在回应战斗而不是随机说气质句 | 英雄台词按 intro/advantage/low_hp/mp_low/interrupt_success/build_trigger/boss_phase/near_defeat 分类触发 | 每个英雄 8 类局势台词，每类至少 3 条；低血、MP 紧张、打断成功有不同输出 | `tests/unit/test_dialogue.py::test_every_hero_has_three_lines_for_each_situational_dialogue_category` 覆盖 6 英雄 8 类中英文各至少 3 条；`tests/unit/test_dialogue.py::test_dialogue_selector_responds_to_distinct_battle_situations` 覆盖 intro/low_hp/mp_low/interrupt_success/build_trigger/boss_phase/near_defeat 触发差异；`tests/unit/test_dialogue.py::test_dialogue_selector_uses_localized_lines` 覆盖中文局势台词；`src/ouro_agent/art/battle_dialogue.py` 使用 `TYPE_CHECKING` 避免 TUI 循环导入 | P0 | C-Polish | done |
| REQ-MODEL-001 | G20/G05/G14 | 玩家看懂模型这一手是聪明、冒险还是偏离策略 | 每次英雄行动展示 Intent/Risk/Align 三元信息，完整思考进入 trace/replay | 主屏不显示长调试思考；同一 turn 可见意图、代价、Prompt/Build 命中或偏离 | `tests/unit/test_tui_frame_builder.py::test_battle_frame_exposes_director_fields`、`tests/unit/test_tui_frame_builder.py::test_battle_screen_keeps_raw_model_reasoning_out_of_main_surface`；`tests/integration/test_mock_battle.py::test_cli_prompt_style_is_visible_in_trace` 验证 trace 写入 `model_analysis`/`model_narration`/`raw_text` | P0 | C-Polish | done |
| REQ-NUMFEED-001 | G20/G10/G14 | 数值变化有阈值感和战术含义 | HP/MP/ATB/CD/护盾展示关键 delta 和阈值提示 | MP 低于关键技能、HP critical、ATB ready、CD locked 至少四类提示可见 | `tests/unit/test_tui_frame_builder.py::test_battle_screen_shows_tactical_resource_thresholds` 验证 HP critical、MP interrupt reserve、ATB ready、CD locked、SHD shield；`tests/unit/test_tui_frame_builder.py::test_battle_screen_width_matrix` 验证宽度稳定 | P0 | C-Polish | done |
| REQ-COUNTER-001 | G20/G02/G10 | 反制窗口清楚，玩家知道何时能打断或破防 | 敌方蓄力、Boss Charge、护盾破防用预告/窗口/结果三段反馈 | 固定 seed 至少出现一次 threat -> window -> result；错过窗口写入战报 | `tests/unit/test_tui_frame_builder.py::test_counter_window_sequence_shows_threat_window_and_result` 覆盖 `[WINDOW] -> [ANSWER] -> [MISSED]`；`tests/unit/test_tui_frame_builder.py::test_boss_frame_exposes_phase_charge_break_enrage` 覆盖 Boss Charge/Break/Enrage；`tests/unit/test_tui_frame_builder.py::test_tactical_diagnosis_counts_missed_counter_and_low_tempo` 验证 missed counter 写入战报诊断 | P0 | D/F | done |
| REQ-CLIMAX-001 | G20/G03/G07/G14 | 打断、击杀、Build 胡牌和 Boss 转阶段有高潮反馈 | 收割、破防、Build Online/High Roll、Codex Reveal、Boss Phase 显示事件横幅 | 大事件不只出现在日志，主屏有横幅或等价强反馈 | `tests/unit/test_tui_frame_builder.py::test_counter_window_sequence_shows_threat_window_and_result` 覆盖打断窗口/回应/错过横幅；`tests/unit/test_tui_frame_builder.py::test_climax_banners_cover_kill_and_build_online` 覆盖 `KILL CONFIRMED` 与 `BUILD ONLINE`/`HIGH ROLL` 高潮面板；`tests/unit/test_codex.py::test_codex_card_observed` 覆盖 `EVENT: CODEX REVEAL`；`tests/unit/test_tui_frame_builder.py::test_boss_phase_crossing_raises_climax_banner` 覆盖 Boss Phase 横幅；`tests/unit/test_tui_frame_builder.py::test_tower_brace_counter_renders_boss_break` 覆盖 Boss Break | P1 | C-Polish | done |
| REQ-BUILDCLIMAX-001 | G20/G03/G14/art08 | Build 胡牌反馈不能每个战斗帧重复刷屏，避免稀释击杀、打断和 Boss 转阶段高潮 | `BUILD ONLINE`/`HIGH ROLL` 高潮面板只在开场无行动帧展示；后续英雄/敌方行动帧保留常规 Build HUD，不重复输出 Build Climax 面板 | 开场帧可见 Build 胡牌；击杀帧只显示 `KILL CONFIRMED`，不再附带重复 `BUILD ONLINE`；固定 seed demo 中 `EVENT BUILD ONLINE` 不逐 turn 刷屏；宽度稳定 | `tests/unit/test_tui_frame_builder.py::test_climax_banners_cover_kill_and_build_online` 覆盖开场 `EVENT BUILD ONLINE`、后续等待帧不重复、击杀帧只显示 `EVENT KILL CONFIRMED` 且无 `BUILD ONLINE/HIGH ROLL`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 中 `EVENT   BUILD ONLINE` 计数为 1 | P0 | C-Polish | done |
| REQ-TUIARCH-001 | G20/Engineering | TUI 能继续扩展而不是在 screens.py 堆字符串 | 建立 Engine Event -> BattleFrame -> BattleScreenModel -> Layout Blocks -> Presenter 架构 | 战斗屏可从 BattleFrame 渲染；`render_battle_screen()` 保持兼容入口 | `tests/unit/test_tui_frame_builder.py::test_battle_frame_flows_through_screen_model_layout_and_presenter` 覆盖 `BattleFrame -> BattleScreenModel -> layout.Block -> PresentedFrame`，并验证 `render_battle_screen()` 兼容入口仍渲染同一行动；`src/ouro_agent/tui/presenter.py` 暴露 `battle_screen_model_from_frame()` 和 `present_battle_screen_model()` | P0 | Tech | done |
| REQ-TUIQA-002 | G20/G19 | 战斗演出有确定性快照和宽度保障 | 建立帧序列、80/100/120、中文/英文、no-animation 快照测试 | 每行 `visual_width(line) <= width`；固定 seed frame 序列稳定 | `tests/unit/test_tui_frame_builder.py::test_no_animation_seed7_frame_sequence_is_stable` 覆盖 seed 7 前 6 个 turn frame 的固定签名、英文/中文、80/100/120 列宽度；`tests/unit/test_tui_frame_builder.py::test_battle_screen_width_matrix` 覆盖单帧中英文宽度矩阵；`tests/integration/test_mock_battle.py::test_cli_play_prints_turn_frames_and_report` 覆盖 CLI `--no-animation` 仍输出 turn frame | P0 | QA | done |
| REQ-TUICANVAS-001 | G22/G14/art08 | TUI 不再靠标点拼接，具备图形化低像素舞台 | 建立 Surface/Canvas 渲染层，支持 text、bar、box、sprite、compose、render | `--unicode` 战斗主舞台使用 block sprite、block lane、block HUD；ASCII fallback 仍可用 | `tests/unit/test_tui_frame_builder.py::test_canvas_surface_draws_block_art_without_width_drift` 覆盖 Surface text/bar/box/sprite/render 宽度；`test_unicode_battle_screen_uses_canvas_block_stage` 覆盖 `THE ECHO ALTAR`、block sprite、effect lane、`GRAPHICAL TUI`；`test_unicode_battle_screen_canvas_width_matrix` 覆盖 80/100/120 列 Canvas、`SELECT/IMPACT/JUDGE`、目标侧 `HIT -16 HP SLN`；`test_unicode_battle_screen_embeds_resource_thresholds_in_canvas` 覆盖 `HP CRIT`、`MP LOW`、`ATB READY` 和 `DELTA MP20>2 I:N HP25/100!`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode` 输出 Canvas 舞台与 ASCII fallback 提示 | P0 | Tech/C-Polish | done |
| REQ-TUICANVAS-002 | G22/art02/art03/art07 | 人物、怪物、武器有可复用图形资产 | 将 block 精灵、弹道、武器 card art 从 `screens.py` 分离到 `src/ouro_agent/art/` | MVP 英雄/怪物/武器有 ASCII fallback 与 Unicode block 资产入口 | `tests/unit/test_tui_frame_builder.py::test_reusable_art_assets_cover_mvp_heroes_enemies_and_weapons` 覆盖 6 英雄、9 敌人 glyph 的 ASCII fallback、Unicode block sprite、`[W:*]` 武器卡、ASCII/Unicode card art、Build 变化和 AI 行为影响；`src/ouro_agent/art/block_sprites.py`、`src/ouro_agent/art/battle_assets.py`、`src/ouro_agent/art/weapon_cards.py` 为复用资产入口；`tests/unit/test_tui_frame_builder.py::test_all_hero_core_poses_have_ascii_sprites` | P0 | C-Polish | done |
| REQ-TUICANVAS-003 | G22/art08 | 窄终端也能看到图形化战斗而不是退回文字摘要 | `unicode_mode=True` 时 80/100/120 列均使用 Canvas battle stage，并响应式调整左右角色列和中央效果 lane | 每行宽度不超过目标列宽；80 列仍可见左右站位、block sprite、节拍条、命中浮字、ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_canvas_width_matrix`；80 列渲染函数样例输出 `GRAPHICAL TUI` 且 `max_width 80` | P0 | C-Polish/QA | done |
| REQ-TUICANVAS-005 | G22/G20/G10 | Canvas 内的数值反馈具有战术意义 | HP/MP/ATB 阈值和资源 delta 进入 Canvas HUD；完整解释仍保留在 ACTION 面板 | 低血、低蓝、ATB ready、MP 消耗后能在 Canvas 第一屏看到短 HUD，不依赖阅读下方日志 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_embeds_resource_thresholds_in_canvas`；`tests/unit/test_tui_frame_builder.py::test_battle_screen_shows_tactical_resource_thresholds` 保留完整 delta 解释 | P0 | C-Polish | done |
| REQ-CANVASSTATUS-001 | G22/G14/G20/art08 | Buff/Debuff 不能只藏在下方列表，主舞台内也要可见 | Unicode Canvas 左右角色 HUD 新增 `FX` 状态芯片，把护盾、沉默、腐化等状态压缩成 `SHD7`、`SLN1`、`CRP2`；同时修正中心 ACTION/JUDGE 文本整体裁剪，防止越界覆盖右侧 HUD | 80/100/120 列不溢出；英雄 Buff 和目标 Debuff 同屏可见；状态芯片不覆盖 HP/MP/ATB、敌方队列、弹道和 ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_status_chips_inside_canvas` 覆盖 80/100/120 列、英雄 `FX SHD7`、目标 `FX SLN1 CRP2`、`TEMPO RAIL`/`RAIL`、`STACK`、ACTION/JUDGE 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出目标侧 `FX SLN1`、后续 `FX CRP1 SLN1`/`FX CRP2 SLN1` 且中心 ACTION 不覆盖右侧 HUD | P0 | C-Polish | done |
| REQ-SKILLRAIL-001 | G22/G10/G20/art08 | 关键技能是否可用不应只在下方 ACTION 面板出现 | Unicode Canvas 英雄侧新增 `SKILL` 小轨道，用技能短码显示前 2-3 个关键技能的 ready、MP 不足或 cooldown 状态 | 80/100/120 列不溢出；`SKILL`、ready 标记和 `HEX` 冷却值在主舞台可见；不覆盖 HP/MP/ATB、FX、中心 ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_skill_rail_inside_canvas` 覆盖 80/100/120 列、`SKILL STG*`、`HEX4`、100/120 列 `FOC*`、ACTION/JUDGE 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出首帧 `SKILL STG* HEX* FOC*`、行动后 `SKILL STG* HEX4 FOC*` 与后续冷却递减 | P0 | C-Polish | done |
| REQ-ENEMYINTENT-001 | G22/G10/G20/art08 | 怪物下一步威胁不应只靠玩家从 ATB、日志和台词推断 | Unicode Canvas 敌方 HUD 新增 `INTENT` 小条，压缩显示 `CHANT`、`SILENCED`、`STRIKE RDY`、`FALTER` 或 `CLEARED` 等当前意图 | 80/100/120 列不溢出；吟唱进度、沉默中断和即将攻击在怪物侧主舞台可见；不覆盖 HP/ATB、FX、RETICLE、STACK 和中心 ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_enemy_intent_inside_canvas` 覆盖 80/100/120 列、`INTENT CHANT 1/2`、`INTENT SILENCED`、`INTENT STRIKE RDY`、`THREAT WINDOW`、`RETICLE [WINDOW]`、ACTION/JUDGE 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `INTENT STRIKE`、`INTENT SILENCED`、`INTENT RELEASE`、`INTENT CLEARED` | P0 | C-Polish | done |
| REQ-HEROPLAN-001 | G22/G05/G10/G20/art08 | 模型这一手的战术意图不应只藏在下方 ACTION 面板 | Unicode Canvas 中心区新增 `PLAN` ribbon，把等待、控制、伤害、守护、反制等意图压缩为主舞台短标签，并标明 Prompt/Build/Counter 来源 | 80/100/120 列不溢出；等待帧、英雄行动帧和敌方压力帧都能在 ACTION/JUDGE 前看到计划；不覆盖弹道、数值、TEMPO、ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_plan_ribbon_inside_canvas` 覆盖 80/100/120 列、`PLAN READ | PENDING`、`PLAN CONTROL | PROMPT HIT`、`PLAN ANSWER | COUNTER`、ACTION/JUDGE 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `PLAN READ | PENDING`、`PLAN CONTROL | PROMPT HIT`、`PLAN ANSWER | COUNTER`、`PLAN BURST | BUILD SKILL` | P0 | C-Polish | done |
| REQ-CANVASBEAT-001 | G22/G10/G19/art08 | 玩家盯着主舞台时也要知道当前 tick 和行动方 | Unicode Canvas 顶部新增 `BEAT` 时钟徽标，显示 `Txxx` 与 `WAIT/HERO/ENEMY` 帧类型 | 80/100/120 列不溢出；等待帧、英雄行动帧、敌方行动帧都能在 Canvas 顶部看到 tick/actor；不覆盖标题事件名和左右 HUD | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_beat_badge_inside_canvas` 覆盖 80/100/120 列、`BEAT T009 WAIT`、`BEAT T009 HERO`、`BEAT T010 ENEMY`、ACTION/JUDGE 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `BEAT T009 WAIT`、`BEAT T009 HERO`、`BEAT T010 ENEMY`、`BEAT T050 WAIT/HERO` | P0 | C-Polish/QA | done |
| REQ-CASTMETER-001 | G22/G10/G20/art08 | 吟唱/蓄力敌人的施法进度要像进度条，而不是只靠文字推断 | Unicode Canvas 敌方侧新增 `CAST` 小槽，用块状条展示 `current/total`，沉默时显示 `CAST CUT` | 80/100/120 列不溢出；吟唱中、沉默切断两种状态在怪物侧主舞台可见；不覆盖 THREAT、RETICLE、HIT 浮字、HP/ATB、FX、INTENT 和 STACK | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_cast_meter_inside_canvas` 覆盖 80/100/120 列、`CAST`、`1/2`、`CAST CUT`、`INTENT CHANT 1/2`、`INTENT SILENCED`、`RETICLE [WINDOW]` 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `CAST ████ 1/1` 和 `CAST CUT`，命中浮字仍显示 `HIT -16 HP` | P0 | C-Polish | done |
| REQ-WOUNDRAIL-001 | G22/G10/G20/art08 | 战斗命中后的敌方血线变化要像可读的斩杀窗口，而不是只看单次浮字 | Unicode Canvas 中心区新增 `WOUND` 轨道，用块状条展示当前目标 HP、刚才伤害和 `HOLD/EXE/DOWN` 状态 | 80/100/120 列不溢出；英雄命中、低血斩杀窗口和击倒状态在主舞台可见；不覆盖场景纹理、弹道、PLAN、ACTION/JUDGE、CAST、HIT 浮字和右侧 HUD | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_wound_rail_inside_canvas` 覆盖 80/100/120 列、`WOUND`、`-16`、`44/60 HOLD`、`12/60 EXE`、`0/60 DOWN`、`HIT -16 HP SLN` 和宽度；`tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_uses_directed_effect_lane_and_hit_pose` 覆盖敌方命中浮字不再与 `[ONLINE] shadow` 串联；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `WOUND -16 ███░ 34/50 HOLD`、`WOUND -15 ░░░░ 8/70 EXE`、`WOUND -47 ░░░░ 0/70 DOWN` 和独立 `HIT -14 HP` | P0 | C-Polish | done |
| REQ-PAINRAIL-001 | G22/G10/G20/art08 | 敌方行动帧要把焦点切回英雄承伤压力，而不是继续显示敌方血线 | Unicode Canvas 中心区在敌方伤害帧显示 `PAIN` 轨道，用块状条展示英雄 HP、刚才承伤和 `SAFE/CRIT/FALL` 状态 | 80/100/120 列不溢出；敌方攻击、低血危险和倒下状态在主舞台可见；不覆盖左侧 HIT 浮字、弹道、PLAN、ACTION/JUDGE、英雄 HP/MP/ATB 和右侧 WOUND/CAST/HUD | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_pain_rail_on_enemy_damage` 覆盖 80/100/120 列、`PAIN`、`-9`、`41/50 SAFE`、`12/50 CRIT`、`0/50 FALL`、`HIT -9 HP`、敌方伤害帧不再显示 `WOUND` 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `PAIN -14 ███░ 85/100 SAFE`，同场英雄命中帧仍输出 `WOUND -16 ███░ 34/50 HOLD` | P0 | C-Polish | done |
| REQ-INTENTCLEAN-001 | G22/G14/G20/art08 | 敌方 Intent 行不能残留上一层 HUD 字符，避免出现 `SILENCEDDY` 这类拼接污点 | Unicode Canvas 敌方 `INTENT` 写入前清理整条 lane，保证它覆盖同一行的 `ATB READY` 残影 | 80/100/120 列不溢出；沉默、吟唱、攻击就绪等 Intent 行没有尾部残影；不影响敌方 HP/ATB、FX、CAST、STACK 和中心 ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_enemy_intent_inside_canvas` 覆盖 80/100/120 列、ATB ready + silence 同行、`INTENT SILENCED` 且无 `INTENT SILENCEDDY` 残影；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出多处干净 `INTENT SILENCED` 和 `CAST CUT` | P0 | C-Polish | done |
| REQ-CUTRAIL-001 | G22/G10/G20/art08 | 反制窗口不能在 Canvas 主舞台里显示被截断的长 `COUNTER CLOCK ... NEXT...` 文本 | Unicode Canvas 中心 effect lane 和 counter 行改用短 `CUT` 轨道，显示进度、FULL/current、READY/CD/MP 状态；完整 `COUNTER CLOCK` 仍保留下方 Action Focus 面板 | 80/100/120 列不溢出；反制窗口、CD 未好、MP 不足和 ready 状态可在主舞台一眼读懂；Canvas 内不出现 `NE...` 截断；不影响 CAST、RETICLE、PLAN、ACTION/JUDGE、WOUND/PAIN | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_marks_counter_window_in_canvas` 覆盖 80/100/120 列、`CUT`、`FULL`、`CD3`、`MP12/18`、`READY`、完整 `COUNTER CLOCK` 仅保留下方面板 1 次且无 `COUNTER CLOCK [#####] FULL | NE...`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `CUT █████ FULL CD3`、`CUT ░░░░░ 0/1 CD1`、`CUT █████ FULL READY`、`CUT █████ FULL MP12/18` | P0 | C-Polish | done |
| REQ-ACTIONLABEL-001 | G22/G16/G20/art08 | Canvas 主舞台的 `ACTION` 行不能把长技能名、目标名或敌人动作截断成低信息量省略串 | Unicode Canvas 中心 `ACTION` 行改用语义短标签，覆盖英雄技能、普攻、守护、敌方普攻、吟唱蓄力、吟唱释放和沉默动作；底部 Battle Film/Action Focus 保留完整 action label | 80/100/120 列不溢出；英雄 `Hex Seal -> Hungry Cultist` 在主舞台显示为 `ACTION HEX -> c`；黑烛侍祭吟唱显示为 `ACTION ENEMY CHARGE`；Canvas 主舞台不出现 `ACTION ...` 截断省略 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_canvas_width_matrix` 覆盖 80/100/120 列 `ACTION HEX -> c`、无 `ACTION Hex Seal` 截断源、并保留完整 `Hex Seal -> Hungry Cultist`；`tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_marks_counter_window_in_canvas` 覆盖 80/100/120 列 `ACTION ENEMY CHARGE` 且无 `ACTION Black Candle Acolyte cha...`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `ACTION HEX -> c`、`ACTION ENEMY CHARGE`、`ACTION ENEMY STRIKE`、`ACTION STING -> k`、`ACTION FOCUS -> HERO` 且 `rg "ACTION .*\\.\\.\\."` 无匹配 | P0 | C-Polish | done |
| REQ-DELTARAIL-001 | G22/G10/G20/art08 | Canvas 主舞台的 `DELTA` 行不能被长敌名或完整说明截断，数值变化要像 HUD 一样短促可扫读 | Unicode Canvas 中心 `DELTA` 行将 MP/HP/ATB/CD/SHD 压缩为无空格短码，例如 `MP72>54 I:Y`、`HP99>85`、`ATB:k96`、`CD:HEX2`；敌方名称用 glyph 压缩；完整资源变化仍保留下方战斗分镜/阈值面板 | 80/100/120 列不溢出；MP 消耗、低血、敌方 ATB ready/near、CD 锁定均不出现 `DELTA ...` 省略串；固定 seed demo 中 `DELTA` 行没有 `...` | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_embeds_resource_thresholds_in_canvas` 覆盖 `DELTA MP20>2 I:N HP25/100!`、HP/MP/ATB 阈值和宽度；`tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_marks_counter_window_in_canvas` 覆盖 80/100/120 列 `DELTA ATB:k96 CD:HEX3` 且无 `DELTA ATB Black Candle`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `DELTA MP72>54 I:Y ATB:H*`、`DELTA ATB:k* CD:HEX3`、`DELTA HP99>85 ATB:k*`，且 `rg "DELTA .*\\.\\.\\."` 无匹配 | P0 | C-Polish | done |
| REQ-WEAPONUI-001 | G22/art07/G03 | 武器图鉴和英雄卡不再只是 `[W:*]` 字段 | 英雄卡显示武器图形卡，包含 `[W:*]` icon、3 行 ASCII/Unicode card art、Build Before -> After 和 AI Effect | 默认英雄卡 ASCII-safe；`hero-card --unicode` 显示 block weapon art；6 英雄都有资产覆盖 | `tests/unit/test_build.py::test_render_hero_card_shows_build_strategy_and_prompt_template` 验证默认 ASCII 武器卡、Build Before -> After、AI Effect；`tests/unit/test_build.py::test_render_hero_card_unicode_shows_block_weapon_art` 验证 Unicode block art；`venv312/bin/ouro --lang en hero-card hero_shadow_apprentice --unicode --content-dir content` 输出 `WEAPON CARD` 与 `░▓███░` | P0 | C-Polish | done |
| REQ-WEAPONGALLERY-001 | G22/art07/G03/G16 | 开局选英雄前能像装备库一样比较武器轮廓、Build 阶段和 AI 倾向 | `list-heroes` 在英雄队列后新增 `WEAPON GALLERY BOARD`，逐把武器显示 `[W:*]` 图标、ASCII 轮廓、当前 Build 阶段、Build shift 与 AI Effect | 玩家不用逐个打开 hero-card 也能比较六把武器的视觉轮廓和战术倾向；默认英文输出 ASCII-safe；英雄选择卡保留 | `tests/unit/test_build.py::test_render_hero_list_en_is_ascii_safe` 覆盖 `WEAPON GALLERY BOARD`、六把 `[W:*]` 武器、ASCII 轮廓、`Build: [ONLINE] shadow/control`、`AI favors interrupts and tempo skills`、下一步 hero-card 指令；`venv312/bin/ouro --lang en list-heroes --content-dir content` 输出 weapon gallery board | P0 | C-Polish/E | done |
| REQ-HEROLOADOUT-001 | G22/G03/G05/G16 | 英雄详情能像开局配置台一样指导玩家进入下一局 | `hero-card` 新增 `HERO LOADOUT BOARD`，汇总 Prompt、Build 阶段、核心标签、风险、开局动作和可执行 run 命令 | 6 英雄详情均显示开局配置板；指定 `--prompt-style control` 时开局动作和命令同步变化；英文默认 ASCII-safe | `tests/unit/test_build.py::test_render_hero_card_shows_build_strategy_and_prompt_template` 覆盖 `HERO LOADOUT BOARD`、`[PROMPT] control`、`[BUILD] [ONLINE]`、`[CORE] shadow / control`、`[OPENER] open by denying chant windows`、run 命令；`test_render_all_six_hero_cards_explain_play_and_risk` 覆盖 6 英雄默认 loadout；`venv312/bin/ouro --lang en hero-card hero_shadow_apprentice --prompt-style control --content-dir content` 输出 hero loadout board | P0 | C-Polish/E | done |
| REQ-INTERACTUI-001 | G22/G18/G19 | 路线、奖励、商店、休息不再像纯字段列表 | 统一 choice card 入口为交互卡追加低像素 action glyph：`[PATH]`、`[REWARD]`、`[SHOP]`、`[REST]`，并保留风险/收益/Build/AI 决策字段 | 四类选择界面在 80/100/120 宽度下不溢出，且玩家能第一眼区分路径、奖励、商店和休整用途 | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `[PATH] ##==>##/##XX##`、`[REWARD] [SEED] -> [PAIR] -> [ONLINE]`、`[SHOP] ##[]##`、`[REST] recover / focus / study`；`tests/unit/test_gameui.py::test_core_screens_have_stable_width_and_required_fields` 覆盖中英文 80/100/120 宽度 | P0 | C-Polish | done |
| REQ-MONSTERUI-001 | G22/G07/art03 | 怪物图鉴不再只是文字解锁表 | Codex monster card 使用低像素怪物轮廓：未知阶段显示 `FOG SILHOUETTE`，观察后显示 `BLOCK SILHOUETTE`，熟悉/掌握阶段显示机制读数和弱点图 | 未知卡不泄露怪物详情；观察/熟悉/掌握阶段逐步显示轮廓、行为、机制和弱点，形成收藏卡反馈 | `tests/unit/test_codex.py::test_codex_card_unknown` 覆盖雾化剪影；`test_codex_card_observed` 覆盖 block silhouette；`test_codex_card_familiar` 覆盖 `MECHANIC READ`；`test_codex_card_mastered` 覆盖 `WEAKNESS MAP`；`env OURO_AGENT_HOME=/tmp/ouro_codex_visual_goal venv312/bin/ouro --lang en codex enemy_black_candle_acolyte --content-dir content` 输出 `FOG SILHOUETTE` | P0 | C-Polish | done |
| REQ-CODEXGALLERY-001 | G22/G07/art03/G16 | 图鉴总览像怪物收藏画廊，能一眼看出未知、已观察和下一步研究动作 | `render_codex_summary()` 在狩猎板后新增 `MONSTER GALLERY BOARD`，逐怪物显示阶段徽章、短 glyph、名称/迷雾、tier、威胁类型、ASCII 轮廓和下一步动作 | 默认 `ouro codex` 不再只是统计和文字列表；未知怪物只显示 `????` 与迷雾轮廓，已观察怪物显示轮廓；总览保持 80 列宽度稳定 | `tests/unit/test_codex.py::test_codex_summary`、`test_codex_summary_counts_family_tier_progress`、`test_codex_summary_no_progress` 覆盖 `MONSTER GALLERY BOARD`、`[??] [c] ????`、`[OB] [k] Black Candle Acolyte`、`silhouette ~(k)~`、下一步怪物卡命令和 80 列宽度；`venv312/bin/ouro codex --lang en --content-dir content` 输出 monster gallery board | P0 | C-Polish/E | done |
| REQ-ANSITHEME-001 | G22/G14/art08 | 终端支持时战斗画面有更强氛围但默认仍干净可测 | 为 Canvas 战斗舞台增加可选 ANSI 语义色彩层，并提供 `--color never/auto/always` | 默认和 `NO_COLOR` 下不输出 ANSI；彩色模式剥离转义后宽度稳定；HP/MP/ATB、反制、高潮等行获得不同语义色 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_optional_color_keeps_canvas_width` 覆盖默认无 ANSI、`always` 彩色、`NO_COLOR` 禁色、剥离 ANSI 后每行 `visual_width <= 100`；`env -u NO_COLOR venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --color always --content-dir content` 输出含 ANSI 且剥离后保留 `GRAPHICAL TUI`/`SELECT`/`JUDGE` | P1 | C-Polish/Tech | done |
| REQ-ACTIONFX-001 | G22/art08/G20 | 战斗动作不再像静态截图，角色和弹道随 turn 明确变化 | 将 Canvas 中央效果 lane 的低像素弹道资产从逻辑中拆出，并按 hero skill、guard、enemy strike、chant、break、counter、climax 选择不同图形；敌方攻击、蓄力、沉默和英雄自目标守护有正确姿态 | 敌方攻击帧可见英雄受击 pose、敌方攻击 pose、反向块面 projectile、独立命中浮字，且不覆盖武器/Build/HUD；自目标守护不误判为受击 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_uses_directed_effect_lane_and_hit_pose` 覆盖 `<██▓▓░`、`STRIKE -9 HP`、英雄 `▐▓x` 受击、`HIT -9 HP` 独立浮字、`[ONLINE] shadow` 未被覆盖和 100 列宽度；`tests/unit/test_gameui.py::test_actor_pose_handles_enemy_director_states_and_self_target_guard` 覆盖 enemy strike/chant charge/silenced/self guard 姿态；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出逐 turn Canvas projectile | P0 | C-Polish | done |
| REQ-SCENEVX-001 | G22/G13/art08 | 战斗第一屏有场景氛围和英雄回应，而不是只有 HUD 与日志 | Canvas battle stage 将 dungeon scene 转成低密度背景纹理，并把局势化英雄台词压缩为左侧 `VOX` 气泡；效果 lane 绘制前清理背景残留 | `VOX`、场景纹理、弹道和命中浮字同屏可见且互不覆盖；背景不能污染 projectile；每行宽度稳定 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_embeds_scene_and_hero_voice_in_canvas` 覆盖 `VOX seal the chant`、`candle`、`broken arch`、`SEAL -16 HP`、无 `▓██>h` 背景残留和 100 列宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 Canvas 第一屏含 `VOX` 与场景纹理 | P0 | C-Polish | done |
| REQ-ENEMYVOX-001 | G22/G13/art08/G20 | 敌人也要参与战斗氛围，而不是只作为数值靶子 | Canvas battle stage 在敌方侧新增确定性 `ENM` 短气泡，按被命中、吟唱、沉默、低血、ATB 将满等局势显示敌方回应 | 英雄 `VOX` 与敌方 `ENM` 同屏，玩家能感到双方对峙；气泡不挤压怪物精灵、威胁标记、命中浮字和 HUD；每行宽度稳定 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_embeds_scene_and_hero_voice_in_canvas` 覆盖 `ENM armor cracking`、英雄 `VOX`、场景纹理、命中效果和 100 列宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出敌方 ENM 气泡 | P0 | C-Polish | done |
| REQ-VOXSHORT-001 | G22/G13/G20/art08 | Canvas 英雄台词不能靠 `...` 截断长句来伪装压缩，英雄回应要短促、局势化、可读 | Unicode Canvas 左侧 `VOX` 改用确定性的短 beat 台词，覆盖开场、打断、攻击、守护、低血、低蓝、敌方蓄力/释放和击杀后局势；原长台词仍可用于下方/非 Canvas 叙事，不进入主舞台截断 | 80/100/120 列不溢出；固定 seed demo 的 `VOX` 行没有 `...`；打断帧显示 `VOX seal the chant`，低蓝帧显示资源紧张短句；不影响 `ENM`、场景纹理、精灵、HUD 和命中浮字 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_embeds_scene_and_hero_voice_in_canvas` 覆盖 `VOX seal the chant`、无旧截断 `VOX There. The wick forg...`、`ENM armor cracking`、场景纹理和 100 列宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `VOX read the room`、`VOX seal the chant`、`VOX needle in shadow`、`VOX brace the rite`、`VOX count sparks`，且 `rg "VOX .*\\.\\.\\."` 无匹配 | P0 | C-Polish | done |
| REQ-BATTLEDIR-001 | G22/G20/art08 | 战斗帧不只好看，还要一眼读出当前威胁、节奏和战术焦点 | 战斗主舞台新增导演 HUD，ASCII compact、ASCII wide 和 Unicode Canvas 都显示 threat/tempo/focus 三要素；Canvas 窄中心区使用压缩 `T/P/F` 标签保证不溢出 | 80/100/120 列中英文均能看到导演读数；反制窗口场景能表达 `WINDOW`、英雄就绪和打断焦点；每行宽度稳定 | `tests/unit/test_tui_frame_builder.py::test_battle_screen_embeds_director_strip_for_current_beat` 覆盖 80/100/120、中英文、ASCII compact 和 Unicode Canvas 的 `DIRECTOR/导演`、`THREAT/T:`、`TEMPO/P:`、`FOCUS/F:`；`tests/unit/test_tui_frame_builder.py::test_battle_screen_width_matrix` 保持主屏宽度矩阵 | P0 | C-Polish | done |
| REQ-MOMENTUM-001 | G22/G10/G16/G20/art08 | 战斗过程要先告诉玩家当前局势势能，而不是让玩家自己从 HP/ATB/日志推断 | 战斗主屏在舞台后新增 ASCII-safe `MOMENTUM BOARD`，汇总 `FLOW`、双方 HP lane、当前目标、本回合 swing 和战术 read | 玩家能一眼判断当前是 WINDOW、PRESSURE、DANGER、ADVANTAGE 还是 EVEN；反制窗口、低血、敌方高 ATB、收尾优势都有不同读法；80/100 列稳定 | `tests/unit/test_tui_frame_builder.py::test_seed7_third_battle_action_result_is_readable` 覆盖 `MOMENTUM BOARD`、`[FLOW] WINDOW`、`[LANE] HERO`、`[SWING] hero hit -19 HP`、`[READ] answer the window` 和 80/100 列宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 momentum board | P0 | C-Polish/F | done |
| REQ-TEMPORAIL-001 | G22/G10/G20/art08 | Canvas 主舞台内直接画出战斗节拍压力，而不是只在下方面板解释 | Unicode Canvas 中心区新增 `TEMPO RAIL` / 窄屏 `RAIL`，用块状轨道展示英雄 ATB、目标 ATB 和当前窗口/压力状态 | 80/100/120 列下轨道不溢出；反制窗口帧能看到 H/E ATB 数值、块状条和 `WIN` 状态；轨道不覆盖弹道、台词、HUD、ACTION/JUDGE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_tempo_rail_inside_canvas` 覆盖 80/100/120 列、`TEMPO RAIL`/`RAIL`、`H100`、`E096`、`WIN`、块状条、弹道和 ACTION/JUDGE 宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `TEMPO RAIL H100`、`E081`、`WIN`、`FIN` 与 `CLR` 状态 | P0 | C-Polish | done |
| REQ-ENEMYSTACK-001 | G22/G10/G16/art08 | 多敌人战斗主舞台内能看到敌方队列，而不是只在下方 roster 读列表 | Unicode Canvas 敌方侧新增 `STACK` 与 `ATB` 两行小型敌方队列轨道，标出当前目标、每名敌人的 HP/ATB 块状压力和隐藏数量 | 80/100/120 列下队列不溢出；当前目标用 `>` 标记；双敌人战斗能在主舞台右侧同时看到 `c` 与 `k` 的 HP/ATB 压力；不覆盖敌人 HUD、威胁徽标和 RETICLE | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_enemy_stack_inside_canvas` 覆盖 80/100/120 列、`STACK`、`ATB`、`-c`、`>k`、`RETICLE [WINDOW]`、`THREAT WINDOW` 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `STACK -c██░ >k███` 与 `ATB   -c███ >k███` | P0 | C-Polish | done |
| REQ-STAGECRAFT-001 | G22/G14/art08 | Canvas 战斗画面像一个有构图焦点的低像素舞台，而不是角色和 HUD 的简单拼接 | Unicode Canvas 新增角色底座阴影、`ACT>`/`<TGT` 行动与目标标记、武器图形小卡、敌方 `THREAT` 徽标和 `RETICLE` 锁定线 | 玩家第一眼能看出谁在行动、目标是谁、当前武器/Build 是什么、敌方威胁是否进入窗口；命中浮字、武器、HUD 和 reticle 互不覆盖；100 列宽度稳定 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_adds_stagecraft_focus_markers` 覆盖 `ACT>`、`<TGT`、`RETICLE [WINDOW]`、`THREAT WINDOW`、`[W:STF] c==*`、`[ONLINE] shadow`、`░▓███░` 与每行宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 stagecraft markers | P0 | C-Polish | done |
| REQ-WEAPONHUD-001 | G22/art08/G03 | Canvas 英雄侧武器/Build 小卡不能把长武器名和 Build 标签截成 `Bl...`、`shadow...`，要像装备 HUD 而不是字段裁剪 | Unicode Canvas 武器小卡改为三行短 HUD：武器 icon、Build 阶段/核心标签、压缩 tag 轨道；窄屏用 `shadow+ctrl` 等短码，宽屏保留 `[ONLINE] shadow` 等可读标签 | 80/100/120 列不溢出；`[W:STF] c==* Bl...` 和 `[ONLINE] shadow...` 不再出现；舞台仍可见武器 icon、block 武器图形和 Build 阶段；不影响 HP/MP/ATB、VOX、ENM 和 ACTION/DELTA | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_canvas_width_matrix` 覆盖 80/100/120 列 `[W:STF] c==*`、窄屏 `shadow+ctrl`、宽屏 `shadow/control`，且无 `[W:STF] c==* Bl...` 和 `[ONLINE] shadow...`；`tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_adds_stagecraft_focus_markers` 保留 100 列 `[ONLINE] shadow`、武器 block art 和 stagecraft；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `[W:STF] c==*`、`[ONLINE] shadow`、`shadow/control` 且无 `Bl...`/`shadow...` | P0 | C-Polish | done |
| REQ-STAGERAIL-001 | G22/G10/G14/G20/art08 | Canvas 主舞台的敌方标题、Impact 摘要和 Tempo Rail 不能靠 `...` 截断关键战斗信息 | Unicode Canvas 右侧敌名使用短职能名，Impact 行改为短 HUD 摘要，Tempo Rail 自适应 bar 长度；完整敌名、impact 和日志仍保留在下方读出区 | 80/100/120 列不溢出；黑烛侍祭标题显示 `ENEMY [k] ACOLYTE` 而不是 `ENEMY [k] Black Candle A...`；命中摘要显示 `HIT-16 MP72>54 INT:N`；tempo rail 不出现 `...`；固定 seed demo 的 Canvas 主舞台无 `...` | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_canvas_width_matrix` 覆盖 80/100/120 列 `ENEMY [c] CULTIST`/`ENEMY [c] HUNGRY CULTIST`、`HIT-16 MP72>72 INT:Y` 和无 `next inter...`；`tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_adds_stagecraft_focus_markers` 覆盖 `ENEMY [k] ACOLYTE` 且无 `ENEMY [k] Black Candle A...`；`tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_tempo_rail_inside_canvas` 覆盖 tempo rail 行无 `...`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `ENEMY [k] ACOLYTE`、`HIT-16 MP72>54 INT:N`、`TEMPO RAIL H036 █░ E100 ██ MISS`，且 `rg "█ .*\\.\\.\\."` 无匹配 | P0 | C-Polish | done |
| REQ-ROSTERCHIPS-001 | G22/G10/G14/G20 | 战斗下方 `ENEMY ROSTER` 不能用完整状态名把敌方队列挤成 `CRP corrup...`、`SLN silenc...` | Unicode `ENEMY ROSTER` 行使用 `FX` 状态芯片，例如 `FX SLN1 CRP1`，并保留死亡标记；完整状态名仍留在 Debuff/详情/战报中 | 80/100/120 列不溢出；多状态敌人 roster 不出现 `corrup...` 或 `silenc...`；玩家仍能看到敌方 HP、ATB、当前目标和状态短码 | `tests/unit/test_tui_frame_builder.py::test_unicode_battle_screen_draws_status_chips_inside_canvas` 覆盖 80/100/120 列 roster `FX SLN1 CRP2` 且无 `corrup...`/`silenc...`；`tests/unit/test_tui_frame_builder.py::test_battle_screen_width_matrix` 保留默认屏完整 `SLN silence(1)` 和宽度；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 roster `FX SLN1 CRP1 (down)`、`FX CRP2 SLN1 (down)`，末尾详情仍保留 `SLN silence(1) id=status_silence` 和 `CRP corrupt(2) id=status_corruption` | P0 | C-Polish | done |
| REQ-BEATFILM-001 | G22/G09/G16/art08 | 战斗证据区从日志堆叠变成可扫读的回合分镜 | `ECHO READOUT` 保留 Echo 成本、本地 trace 和 Action Strip，同时把模型行动、裁判结果、最近战斗日志组织为 `[01 MODEL]`、`[02 JUDGE]`、`[03 LOG]` 三格 `BEAT FILM` | 80/100/120 列中英文均显示分镜；中文用本地化技能/敌人名；窄屏日志可省略但必须保留事件主体；不泄露 raw model reasoning | `tests/unit/test_tui_frame_builder.py::test_battle_readout_uses_beat_film_instead_of_loose_log_dump` 覆盖 80/100/120、中英文、`MODEL TURN/模型行动`、`BEAT FILM/战斗分镜`、三格分镜、行动、裁判、日志和宽度；`tests/integration/test_mock_battle.py::test_battle_screen_is_ascii_safe` 保持默认 ASCII-safe 与 `MODEL TURN` | P0 | C-Polish/E | done |
| REQ-BEATLOG-001 | G22/G09/G16/G20 | Beat Film 的 `[03 LOG]` 不能把长战斗日志裁成 `-MP ...` 或 `sile...`，要像回合分镜短字幕 | `[03 LOG]` 将常见战斗日志压缩为事件短码，例如 `CAST HEX -MP18`、`CHANT BREAK`、`STRIKE -7`、`SLN CULTIST`；完整日志仍保留在 state log/trace 与其它复盘证据中 | 80/100/120 列不溢出；固定 seed demo 的 `[03 LOG]` 行没有 `...`；技能、吟唱、护盾、命中和沉默事件主体都可扫读 | `tests/unit/test_tui_frame_builder.py::test_battle_readout_uses_beat_film_instead_of_loose_log_dump` 覆盖 80/100/120 与中英文 `CAST HEX HIT-16`、`SLN CULTIST` 和 `[03 LOG]` 无 `...`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `[03 LOG] CAST HEX -MP18`、`CHANT +1`、`STRIKE -7`、`SHD -8`、`CHANT BREAK`、`CAST STING -MP12`，且 `rg "\\[03 LOG\\].*\\.\\.\\."` 无匹配 | P0 | C-Polish | done |
| REQ-BEATJUDGE-001 | G22/G09/G10/G20 | Beat Film 的 `[02 JUDGE]` 不能重复伤害和长 impact 字段，例如 `VALID` 后重复 `-16 HP` 并追加长 MP/interrupt 文案 | `[02 JUDGE]` 将裁判状态与 impact 压缩为短字幕，例如 `VALID HIT-16 MP72>54 INT:N`、`LOCAL CHARGE WIN:OPEN`、`LOCAL RELEASE HP99>85 CAST:REL`；完整 Judge/Impact 仍保留在 Action Focus 面板和 trace | 80/100/120 列不溢出；`[02 JUDGE]` 行不重复伤害片段；固定 seed demo 的 `[02 JUDGE]` 行没有 `next interrupt` 长句 | `tests/unit/test_tui_frame_builder.py::test_battle_readout_uses_beat_film_instead_of_loose_log_dump` 覆盖 80/100/120 与中英文 `VALID HIT-16 MP72>72 INT:Y`/`有效 HIT-16...`、无重复伤害与无 `next interrupt`；`venv312/bin/ouro --lang en play --mock --seed 2 --no-animation --no-trace --unicode --content-dir content` 输出 `[02 JUDGE] Judge: VALID HIT-16 MP72>54 INT:N`、`LOCAL CHARGE WIN:OPEN`、`LOCAL RELEASE HP99>85 CAST:REL`，且三条 `rg` 坏味道搜索均无匹配 | P0 | C-Polish | done |
| REQ-ROUTEMAP-001 | G22/G04/G18 | 路线选择不再只是卡片列表，先呈现可比较的路径结构 | 路线选择页新增 ASCII-safe `ROUTE MAP` 像素地图，显示当前楼层节点、可选编号、已访问 `X`、节点类型和风险压力 | 玩家先看到路径结构和分支压力，再阅读路线卡详情；已访问节点不再参与编号，80/100 列不溢出 | `tests/unit/test_gameui.py::test_route_choice_shows_pixel_route_map_with_visited_nodes` 覆盖 `ROUTE MAP`、图例、`[X:C]` 已访问、`[1:$]` 商店、`[2:+]` 休整、`Branch pressure: 2 choices`、`+-->/|-->` 路径线和 80/100 列宽度；`tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖路线页同时保留 `[PATH]`、风险收益、Build fit 和决策信息；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 route map | P0 | C-Polish/D | done |
| REQ-ROUTEDIR-001 | G22/G04/G18/G20 | 路线选择要先给出跨分支导演判断，而不只在卡片内各自推荐 | 路线页在 Route Map 后新增 `ROUTE DIRECTOR BOARD`，按可选节点输出 `TAKE/FIX/GREED/RISK/FLEX/WAIT`、风险 badge、节点名和压缩推荐理由 | 玩家能先比较所有分支的行动性质，再阅读路径卡；shop/rest 显示 `FIX`，危险战斗显示 `RISK/GREED`；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `ROUTE DIRECTOR BOARD` 和 80 列宽度；`tests/unit/test_gameui.py::test_route_choice_shows_pixel_route_map_with_visited_nodes` 覆盖 shop/rest 分支 `[1] FIX`、`[2] FIX`、英文 80/100 和中文 80 列 `路线导演板`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 route director board | P0 | C-Polish/D | done |
| REQ-REWARDFX-001 | G22/G03/G18 | 奖励选择第一眼能看出 Build 变化，不必逐行读卡片 | 奖励页新增 ASCII-safe `REWARD BUILD TRACK`，按选择编号展示当前阶段、拿后阶段、标签/图鉴/经济/治疗轨道；修复 Affix 名称含 Focus 时误显示 REST 图标 | 玩家先看到 `[1] [ONLINE] => [ONLINE]`、标签变化和 Codex/Gold/Heal 功能轨道，再读奖励卡详情；奖励卡不再被误分类为休息卡；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_reward_choice_build_track_stays_width_stable` 覆盖英文 80/100/120、中文 80 列和 `REWARD BUILD TRACK`/`奖励 Build 轨道`；`tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `Current: [ONLINE] Online`、`[1] [ONLINE] => [ONLINE] | tags shadow 5->6`、`[2] ... corruption`、`[3] [CODEX] => [+1 STUDY]`、保留 Build before/after 与 AI impact、且 reward 中不出现 `[REST] ##+##`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 reward build track | P0 | C-Polish/D | done |
| REQ-PICKPRIO-001 | G22/G03/G18/G20 | 奖励选择不仅展示变化，还要给出可执行优先级判断 | 奖励页在 Build Track 后新增 `PICK PRIORITY BOARD`，按选项输出 `BEST/CORE/PIVOT/INFO/ECON/SAFE/FLEX`，区分阶段推进、主标签、转向、情报、经济和生存线 | 玩家能在读详情前先判断哪个奖励更贴当前 Build；80/100/120 与中文不溢出；中文显示 `选择优先级面板`、`主标签` 和 `提示词情报` | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `PICK PRIORITY BOARD`、`[1] CORE`、`[2] CORE`、`[3] INFO`；`tests/unit/test_gameui.py::test_reward_choice_build_track_stays_width_stable` 覆盖英文 80/100/120、中文 80 列和 `选择优先级面板`、`主标签`、`提示词情报`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 pick priority board | P0 | C-Polish/D | done |
| REQ-SHOPFX-001 | G22/G04/G18 | 商店第一眼能看出它是局内纠错站，而不是普通商品列表 | 商店页新增 ASCII-safe `SHOP FIX BOARD`，按 Recover/Build/Prompt/Scout 四类货架汇总价格、READY/LOCKED 和修正方向 | 玩家先判断是否买恢复、补 Build、换 Prompt 或买侦察，再阅读商品卡；低金币时明确 LOCKED，80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_shop_fix_board_tracks_affordability_and_lanes` 覆盖 `SHOP FIX BOARD`、`RECOVER 12g LOCKED`、`PROMPT 10g LOCKED`、`SCOUT 8g READY`、英文 80/100/120 和中文 80 列；`tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 READY 状态下 `RECOVER/BUILD/PROMPT/SCOUT` 四类行、保留 `Build before/after`、`AI impact`、`Restore MP to full` 和 `Boss clue`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 shop fix board | P0 | C-Polish/D | done |
| REQ-SHOPBUDGET-001 | G22/G04/G18/G20 | 商店不只显示能不能买，还要帮玩家判断金币该怎么花 | 商店页在 Fix Board 后新增 `BUDGET TACTICS BOARD`，展示钱包、购买后剩余金币、`BUY/THINK/WAIT` 预算判断和原因 | 高金币时能看到恢复/侦察等可买建议；低金币时显示缺口并推荐低价侦察或存钱；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `BUDGET TACTICS BOARD`、`Wallet: 25g`、`[1] BUY RECOVER 12g -> 13g`、`[5] BUY SCOUT 8g -> 17g`；`tests/unit/test_gameui.py::test_shop_fix_board_tracks_affordability_and_lanes` 覆盖低金币 `WAIT`、`short 3g`、`short 1g`、`SCOUT 8g -> 1g`、中文 `预算战术面板` 和 `缺 3g`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 budget tactics board | P0 | C-Polish/D | done |
| REQ-RESTFX-001 | G22/G04/G18 | 休息三选一能一眼看出互斥后果，而不是只列按钮 | 休息页新增 ASCII-safe `REST DECISION RING`，在选项卡前汇总 Recover/Focus/Study 的资源修复、下一战护盾和机制侦察结果 | 玩家先看到三种休整路线的后果，再决定是否恢复、抗压或研究；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_rest_decision_ring_shows_three_mutually_exclusive_outcomes` 覆盖 `REST DECISION RING`、`RECOVER`、`FOCUS`、`STUDY`、英文 80/100/120 和中文 80 列；`tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `HP 33->63 / MP 0->72`、`SHD 12`、`scout note`，并保留原 `[1] Recover/[2] Focus/[3] Study` 卡片；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 rest decision ring | P0 | C-Polish/D | done |
| REQ-RESTPRIO-001 | G22/G04/G18/G20 | 休整三选一要给出当前状态下的优先级，而不只是列出互斥后果 | 休息页在 Decision Ring 后新增 `REST PRIORITY BOARD`，根据当前 HP/MP 给 Recover/Focus/Study 标记 `PICK/FLEX/LOW`，并展示资源变化或适用场景 | 低血低蓝时 Recover 标为 `PICK`；Focus 和 Study 仍可见但优先级较低；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_route_reward_shop_rest_screens_show_decision_context` 覆盖 `REST PRIORITY BOARD`、`[1] PICK RECOVER`、`[2] FLEX FOCUS`、`[3] LOW STUDY`；`tests/unit/test_gameui.py::test_rest_decision_ring_shows_three_mutually_exclusive_outcomes` 覆盖英文 80/100/120、中文 80 列、`休整优先级面板` 和 `PICK RECOVER`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 rest priority board | P0 | C-Polish/D | done |
| REQ-EVENTFX-001 | G22/G04/G18 | 事件页不再是普通 Choices 列表，能先看清命运分支 | 事件页新增 ASCII-safe `EVENT FATE BOARD`，按 Build/Gold/Heal/Codex 等结果汇总选项，并统一事件奖励卡图标 | 玩家先看到事件会把本局推向 Build、经济还是生存；事件选择卡不再误用 `[PATH]` 图标；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_event_fate_board_summarizes_event_outcomes` 覆盖 `EVENT FATE BOARD`、`BUILD => tags hunter 0->1`、`GOLD => +50 shop power`、`HEAL => HP 50->100`、事件卡 `[REWARD] [SEED] -> [PAIR] -> [ONLINE]`、无 `[PATH] ##==>##` 误分类、英文 80/100/120 和中文 80 列；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 event fate board | P0 | C-Polish/D | done |
| REQ-EVENTRISK-001 | G22/G04/G18/G20 | 事件选择要把高收益、修复和情报风险讲清楚 | 事件页在 Fate Board 后新增 `EVENT RISK BOARD`，按选项输出 `TAKE/GREED/SAFE/INFO/THINK/FLEX` 与原因，结合当前 HP 和 Build 标签判断 | 半血事件中治疗显示 `SAFE`，金币显示 `GREED`，非核心 Build 奖励显示 `GREED/pivot`；80/100/120 与中文不溢出 | `tests/unit/test_gameui.py::test_event_fate_board_summarizes_event_outcomes` 覆盖 `EVENT RISK BOARD`、`[1] GREED => pivot tags hunter 0->1`、`[2] GREED => funds shop fixes`、`[3] SAFE => HP line is stressed`、英文 80/100/120、中文 80 列 `事件风险面板` 和 `血线有压力`；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 输出 event risk board | P0 | C-Polish/D | done |
| REQ-RUNRESULTUI-001 | G22/G16/G18 | 结算页能一眼复盘本局结果和下一步，而不是只列统计字段 | 即时 run 结算新增 ASCII-safe `RUN RESULT BOARD`，汇总胜负、楼层、节点数、战斗胜负、资源、Build 阶段和下一局提示 | 玩家死亡或通关后第一屏能看到本局复盘和重开方向；80/100/120 不溢出；通关和死亡有不同结果 badge | `tests/unit/test_gameui.py::test_run_summary_shows_result_board_for_retry_decision` 覆盖 `RUN RESULT BOARD`、`[FALL]`、`[WIN]`、`3W/1L`、资源、Build、retry 提示和 80/100/120 宽度；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 结算输出 run result board | P0 | C-Polish/E | done |
| REQ-RUNRETRY-001 | G22/G09/G16/G18 | 结算后直接给出下一局配置，而不只说一句“复盘后重试” | 即时 run 结算在 Result Board 后新增 `RETRY LOADOUT BOARD`，把 Prompt、Seed、Route、Codex/Build 拆成重开行动槽；死亡与通关给不同方向 | 死亡结算给 control、固定 retry seed、rest/shop、Codex 缺口；通关结算给 guarded、压力 seed、elite/event greed 和 winning build 对比；80/100/120 不溢出 | `tests/unit/test_gameui.py::test_run_summary_shows_result_board_for_retry_decision` 覆盖 `RETRY LOADOUT BOARD`、死亡 `[PROMPT] control`、`[SEED] 8`、`[ROUTE] rest/shop`、通关 `[PROMPT] guarded`、`[ROUTE] elite/event greed line` 和 80/100/120 宽度；`venv312/bin/ouro --lang en run --mock --seed 7 --no-animation --auto --no-trace` 结算输出 retry loadout board | P0 | C-Polish/E | done |
| REQ-RUNREPORTUI-001 | G22/G09/G16 | 回看报告不只总结上一局，还要像下一局配置卡 | `run-report` 新增 ASCII-safe `NEXT RUN LOADOUT`，把 Prompt、Seed、Codex 缺口和 Route 建议拆成下一局行动槽 | 死亡报告给出 control/固定 seed/Codex/rest-shop；通关报告给出 guarded/压力 seed/构筑比较/高收益路线；80/100/120 与中文不溢出 | `tests/unit/test_archive.py::test_cli_runs_shows_persisted_archives` 覆盖 latest death report 的 `NEXT RUN LOADOUT`、`[PROMPT] control`、`[SEED] 23`、`[CODEX] patch`、`[ROUTE] rest/shop`，以及 complete path report 的 guarded/elite-event 建议；`tests/unit/test_archive.py::test_run_report_next_loadout_width_matrix` 覆盖英文 80/100/120 和中文 80 列；`venv312/bin/ouro run-report --lang en --content-dir content` 输出 next run loadout | P0 | C-Polish/E | done |

---

## 5. 需求变更规则

1. 新需求必须写入本矩阵。
2. 改需求必须保留原 ID，不随意重命名。
3. 删除需求要标记为 cancelled，不直接抹掉。
4. 实现完成后状态改为 done，并补充证据路径。
5. 如果需求影响设计基线，必须同步更新 `02_设计基线与术语表_20260503.md`。
